# propose 技能 — 措辞篇幅与冗余评估 + 精简方案

> 评估对象：`assets/zh/skills/propose/` 技能包（生成 OpenSpec 四件套）
> 评估方式：只读审查（五阶段框架 + 11 维标准），**未修改任何技能文件**
> 评估焦点：措辞篇幅是否过长、是否存在冗余/重复/可合并内容、如何削减字数而不损失关键指令

---

## 一、总体结论

**结论：篇幅偏长，存在可量化的中重度冗余；但核心流程、硬闸口与产物约束完整、无阻断性缺陷。属「可用但需优化（需修改）」档，非「需重做」。**

- 主要问题不是"写了不该写的东西"，而是"同一件事被反复重述"：Mode A/Mode B 语义在 SKILL.md 与 `artifact-batch-generation.md` 间被陈述约 **40 次**；"AI 禁止代选模式""主代理禁止自审冒充""propose-reviewer 缺失阻断"等同类禁令在两层各写一遍；Mode A 单批流程图被画了两次。
- 这些重复**不承载新约束**，删并后执行效能、四件套输出结构、产物质量门禁均不受影响。
- 预估可削减：SKILL.md 387→约 300 行（-22%），policy 297→约 240 行（-19%）；两份正文合计约 684→约 540 行，中文字符约 4968→约 3900（-21%）。**全部削减均为"重述/重复/可合并"，不删任何闸口、路径、命令或输出规则。**

---

## 二、量化基线

| 文件 | 行数 | 字符(含空白) | 中文字符 |
|---|---|---|---|
| SKILL.md | 387 | 13,759 | 2,727 |
| policies/artifact-batch-generation.md | 297 | 7,658 | 2,241 |
| templates/design-template.md | 67 | 806 | 143 |
| templates/tasks-template.md | 120 | 3,607 | 984 |
| **正文合计（SKILL+policy）** | **684** | **21,417** | **4,968** |

冗余短语出现频次（SKILL.md + policy 两文件合并统计）：

| 重复的语义/短语 | 出现次数 | 说明 |
|---|---|---|
| `per_batch` / `§4` / 批内审查（Mode 语义） | ~40 | 同一概念反复陈述 |
| 硬编码结构 / 未读 openspec instructions | 6 | 同一条禁令两层各写 |
| 禁止 AI 代选模式 | 3 | 同义，分散在三处 |
| 主代理禁止自审冒充 | 3 | 同义，两层各写 |
| 提示 `polaris-flow init/update` | 2 | 逐字相同 |

---

## 三、可移除 / 精简的具体内容类型（带位置与示例）

### 类型 1 — Mode A/B 语义的多次重述 → 合并为单一权威陈述 + 引用
同一句"per_batch 走 §3+§4，after_all 只走 §3、跳过 §4"在以下位置重复：
- SKILL.md 3.3.2 决策表（L213–216）已完整映射 A/B → policy 路径。
- **SKILL.md 3.4 执行范围表（L224–227）逐字复述同一映射** → 可改为一行指针："执行范围见 3.3.2 表，勿在本 skill 另写循环。"
- SKILL.md 4.2（L277–278）重述"Mode A 齐套 Verdict / Mode B 唯一主审" → 压缩为一句引用 3.3。
- policy 核心原则（L10–11）、§1 表（L27–32）、§3.2（L91–92）、§4（L100）、§6（L295–296）五处重复同一行为。保留 **核心原则 L10–11 为唯一权威句**，§1 表、§3.2、§6 的模式分支行改为引用。
- **预估削减：约 25–35 行。**

### 类型 2 — 同类"禁止"约束在两层各写一遍 → 单点定义 + 引用
- "主代理禁止自审冒充"：SKILL.md L231（3.4 禁止清单）+ policy L12。→ 保留 policy L12 为权威，SKILL.md 改为"遵守 policy 自审冒充禁令"。
- "未读 openspec instructions 就硬编码"：SKILL.md HARD-GATE L14 + policy L8。→ 保留 policy L8，HARD-GATE 改为引用。
- "propose-reviewer 缺失 → 阻断，提示 polaris-flow init/update"：SKILL.md L281 与 policy L124 **逐字相同**。→ 保留一处，另一处引用。
- **预估削减：约 6–10 行。**

### 类型 3 — "禁止 AI 代选模式"三处同义 → 合并
- SKILL.md L169、L218；policy L32。→ 在 3.3.2 决策点保留唯一权威句，其余两处改为"见 3.3.2，AI 不得代选"。
- **预估削减：约 3–4 行。**

### 类型 4 — Mode A 流程图被画两次 → 合并
- policy §4.6 流程串（L237–246）与 §5.3 模式 A 批次流水线（L274–288）是同一单批流程的两张图。→ 保留 §5.3 完整图，§4.6 删除或内联为一行。
- **预估削减：约 10 行。**

### 类型 5 — 4.2 / 4.3 的 materials 列表近重复 → 引用
- SKILL.md 4.2 materials（L292–299，6 项）与 4.3 materials（L321–327，6 项）除 4.3 首项为 PrimaryReport 外几乎一致。→ 4.3 改为"materials 同 4.2，首项替换为 PrimaryReport（D-2 下全文拼入）"。
- **预估削减：约 6 行。**

### 类型 6 — 3.3.1 推荐判定信号表可压缩
- SKILL.md L171–189 两张信号表（推荐 A / 推荐 B，约 9 行说明）编码了判断依据但措辞冗长。→ 保留全部信号点，改为紧凑决策启发式（每条一行），不丢信号。
- **预估削减：约 10–15 行。**

### 类型 7 — 续跑/恢复说明分散 → 集中一处
- SKILL.md L32、L54、L121、L372–376 四处讲"续跑/恢复"，触发点不同但可归集为单一「续跑」小节，用列表区分触发条件。
- **预估削减：约 5–8 行。**

### 类型 8 — 强调性/解释性冗余句
- SKILL.md L229"完成条件见 policy §6…机械终检在 Step 4.1，不在本步 / 不在 policy"与 3.4 标题重复。
- SKILL.md L169"禁止用推荐直接写入"与 L218"用户选择优先于推荐"同义，合并。
- L271 fallback 注记可收紧一句。
- **预估削减：约 5–8 行。**

> 合计预估削减 **约 70–96 行（占正文 10–14%），中文字符约 1000+**。templates 两个文件（design/tasks）属输出契约，非冗余散文，**不建议删减**。

---

## 四、必须保留的核心要素（不可删）

以下任一删除都会直接损害执行效能、输出结构或产物质量，**不在精简范围**：

1. **HARD-GATE 四条硬闸口**（intention 存在性检查 / 生成 tasks 前必读 tasks-template / 必须走 artifact-batch-generation policy / 禁用 `superpowers:using-git-worktrees`）— L9–16。
2. **标识约定**全部路径与 `change_id` 主键定义（L20–32）— 正确性基石。
3. **流程顺序 Step 0→5 及其阻塞条件**（零匹配阻断、intention 缺节阻断、tasks-lint 失败阻断等）— 控制流不可省。
4. **3.3 模式选择逻辑**：推荐判定 + 决策点 + 写入 `state.yaml` 的 `artifact_review_mode` — 仅压缩措辞，不删信号与"用户优先于推荐"原则。
5. **3.5 迁入命令与"禁止在 .polaris 保留副本"**（L240–250 的 `mv` 命令与校验）— 确定性 bash，必须原样保留。
6. **4.1 机械终检清单**（四件套存在性、proposal/design 必需节、`tasks-lint` 必跑）— L254–271，质量门禁核心。
7. **4.2 主审派发**（subagent-probe、`propose-reviewer`、materials 顺序、落盘路径）— 评审出口不可省。
8. **4.4 消化规则**（BLOCK/APPROVE_WITH_CONCERNS、重跑上限 3 轮、动过四件套须先重跑 4.1）— L332–338。
9. **policy §3 标准生成循环**（openspec status / instructions / 消费 JSON 载荷 / 失败处理）— 真实落盘逻辑。
10. **policy §4 反思循环**（4a 通过原则 / 4b 修复循环 / 4c 最大轮次 `artifact_max_round` 缺省 5 / review-log 结构与轮次计数）— 这是批内审查引擎，全部保留。
11. **policy §5 冻结/解冻**（5.1 allow/forbid 表、5.2 解冻级联）— 决策性内容保护规则，必须原样。
12. **templates/tasks-template.md 与 design-template.md 全部规则** — 输出契约，非冗余。

---

## 五、精简方案（合并映射 + 执行顺序）

| 步骤 | 动作 | 影响文件 | 预估削减 |
|---|---|---|---|
| 1 | Mode A/B 行为收敛为 policy 核心原则唯一权威句，SKILL 3.4/4.2 与 policy §1/§3.2/§6 改为引用 | SKILL + policy | ~25–35 行 |
| 2 | 同类禁止约束去重：自审冒充、硬编码、init/update 阻断各保留单点 | SKILL + policy | ~6–10 行 |
| 3 | "AI 禁止代选"三处合并为一处权威句 | SKILL + policy | ~3–4 行 |
| 4 | Mode A 流程图合并（删 §4.6 或 §5.3 之一） | policy | ~10 行 |
| 5 | 4.3 materials 改为引用 4.2 | SKILL | ~6 行 |
| 6 | 3.3.1 信号表压缩为紧凑启发式 | SKILL | ~10–15 行 |
| 7 | 续跑/恢复说明归集为单小节 | SKILL | ~5–8 行 |
| 8 | 删除强调性/解释性冗余句 | SKILL | ~5–8 行 |

**总预估：约 70–96 行 / 中文字符约 1000+，正文降幅约 10–14%，零关键指令损失。**

执行纪律（若后续进入写入确认）：每步最小侵入、单点定义后引用、改完通读确认无断点、保留全部 HARD-GATE 与门禁原文。

---

## 六、11 维评分（聚焦相关维度，附证据）

| # | 维度 | 评分 | 证据 |
|---|---|---|---|
| 9 | 参考资料质量（单一信息源/上下文成本） | **6** | Mode 语义跨两文件重述 ~40 次（L10–11/27–32/91–92/100/213–227/277–278）；同类禁止约束两层重复（L14 vs L8、L231 vs L12、L281 vs L124）；上下文成本高 |
| 5 | 工作流程完整性 | 9 | Step 0→5 无断点，阻塞/恢复/降级分支齐全 |
| 6 | 输出可执行性 | 9 | tasks-template 含模板+强制规则+lint；design-template 完整；4.1 有可判定通过条件 |
| 7 | 边界与停止条件 | 9 | HARD-GATE、4c 人工决策、4.4 重跑上限、Mode B 禁跑 §4 均明确 |
| 8 | 复杂度适配 | 8 | 复杂度与任务匹配；冗余属"表述"而非"机制过度" |
| 11 | 安全与隐私 | 9 | 无硬编码密钥/外发/静默破坏；高风险操作均有确认与阻断 |
| 1–4,10 | 选题/定位/触发/增量/脚本 | 8–9 | 非本次焦点，均达标；本技能无独立脚本（N/A 不适用，因其依赖外部 hooks 非包内脚本） |

**跨维度一致性检查**：
- 维度 5/6/7 高分 ↔ 维度 9 低分：流程正确但"表述层"重复拉高上下文成本，二者可共存，印证问题在措辞不在逻辑。
- 维度 8 与冗余：机制本身适配，冗余纯属重述，精简不改变复杂度适配结论。

**综合判定**：五阶段结构/安全通过；无 P1；但维度 9 < 8 → 落入「需修改」档（非阻断）。必要触发测试/脚本实测因本次为只读评估未执行，标注为"未验证"。

---

## 七、其他发现（非篇幅问题，需核实，建议并入后续修复）

- **P2 — 断裂/错位引用**：SKILL.md L132 引用 `./templates/intention-template.md`，但该文件实际位于 `clarify/templates/intention-template.md`，propose 作用域内路径不存在。应改为正确跨技能路径或复制副本。
- **P2 — 缺失 policy 引用**：SKILL.md 引用 `./policies/decision-point.md`、`ask-question-react.md`、`outside-voice.md`、`auto-transition.md`，在 skills 树中均未找到（仅 `subagent-delegate-policy.md` 存在于 subagent-probe）。若这些由运行时 `PLUGIN_ROOT` 解析则无碍，否则为断裂引用，需确认其真实位置并补全或修正路径。
- 上述两项不影响本次"篇幅冗余"结论，但属准确性缺陷，建议与精简同步处理。

---

## 八、影响评估（为何削减不影响效能/结构/质量）

- **执行效能**：精简仅删"重述与重复"。每处执行分支（Step 顺序、policy §3/§4/§5、hooks 调用）原文保留，模型读取路径不中断。
- **输出结构**：四件套路径、文件名、落盘顺序（proposal→specs→design→tasks）、review-log/reviews 目录均原样保留。
- **产物质量**：4.1 机械终检、`tasks-lint`、宪法对齐/备选方案/前提等必需节、冻结/解冻规则全部保留，质量门禁无削弱。
- **风险点**：去重后必须保证"引用"真正可达（单点定义文件/章节存在），否则会引入新的断裂引用——这正是第七节的 P2 需先核实的原因。

---

*本报告为只读评估，未改动 `propose` 技能任何文件。如确认采纳，请进入「写入确认」阶段，由我按第五节方案逐条备份并最小侵入修改。*
