# propose 技能 — 措辞篇幅与冗余评估 + 精简方案（刷新版）

> 评估对象：`assets/zh/skills/propose/` 技能包（生成 OpenSpec 四件套）
> 评估方式：只读审查（五阶段框架 + 11 维标准），**未修改任何技能文件**
> 更新说明：**2026-08-20 已按本评估优化 `policies/artifact-batch-generation.md`（297→286 行）**；本版为最新状态评估，聚焦「仍可在 SKILL.md 削减的冗余」与整体剩余建议。
> 评估焦点：措辞篇幅是否过长、冗余/重复/可合并内容、如何削减而不损失关键指令。

---

## 一、总体结论（最新状态）

**结论：整体已改善，但 SKILL.md 仍存在中重度冗余，可继续精简；核心流程、硬闸口与产物约束完整，无阻断性缺陷。属「需修改（非阻断）」档。**

- 已完成：`artifact-batch-generation.md` 已去重（删模式→路径表、模式分支重述、重复流程图，改为引用「核心原则」与 §5.3），削减 11 行，门禁零损失。
- 仍待处理：SKILL.md 内部 Mode A/B 语义仍被多次重述、与 policy 重复的同类禁令、4.2/4.3 的 materials 近重复、3.3.1 信号表偏长、续跑说明分散——这些均为「重述/重复」，删并不影响执行。

| 文件 | 当前行数 | 中文字 | 备注 |
|---|---|---|---|
| SKILL.md | 379 | 2,612 | 未改动，含主要剩余冗余 |
| policies/artifact-batch-generation.md | 286 | 2,221 | 已优化（-11 行） |
| templates/design-template.md | 67 | 143 | 输出契约，不建议删 |
| templates/tasks-template.md | 120 | 984 | 输出契约，不建议删 |
| **正文合计** | **665** | **4,833** | policy 优化后；SKILL 仍可削 ~40 行 |

冗余短语当前频次（SKILL.md + policy 合并）：`per_batch/§4/批内审查` SKILL 12 + policy 23；`代选` 各 1；`自审冒充` 各 1；`openspec instructions/硬编码` SKILL 1 + policy 4；`polaris-flow init/update` 各 1。

---

## 二、已完成项（policy 文件，2026-08-20）

| # | 位置 | 改动 | 保留 |
|---|---|---|---|
| 1 | §1 模式→路径表 | 删 4 行表，改为引用「核心原则」 | 进入前提校验、「禁止 AI 代选」 |
| 2 | §3.2 模式分支 | 两子弹点改为引用「核心原则」 | `/opsx:propose` 不得替代循环 |
| 3 | §4.6 流程图 | 删 10 行重复图，改为指向 §5.3 | §5.3 完整流水线图 |
| 4 | §6 收尾句 | 轻量收紧 | Mode A/B 完成条件 |

备份：`policies/artifact-batch-generation.md.bak.2026-08-20`（297 行，可回滚）。

---

## 三、仍可移除 / 精简的具体内容类型（SKILL.md，带位置）

### 类型 1 — Mode A/B 语义在 SKILL.md 内多次重述 → 合并
- 3.3.2 决策表（L216–217）已完整映射 `per_batch`→§3+§4 / `after_all`→仅 §3。
- **3.4 执行范围表（约 L224–227）逐字复述同一映射** → 改为一行指针「执行范围见 3.3.2 表，勿另写循环」。
- **4.2 模式语义（约 L277–278）重述「Mode A 齐套 Verdict / Mode B 唯一主审」** → 压缩为一句引用 3.3。
- 预估削减：约 6 行。

### 类型 2 — 与 policy 重复的「禁止」约束 → SKILL 改为引用 policy
- HARD-GATE L14：「禁止硬编码四件套结构；禁止主代理自审冒充制品审查」与 policy L8 / L12 同义 → 保留政策为权威，SKILL 改为「遵守 policy 中硬编码/自审冒充禁令」。
- 4.2 L281：「缺失 → 阻断，提示 `polaris-flow init/update`」与 policy L124 逐字相同 → 改为引用 policy。
- 预估削减：约 3 行。

### 类型 3 —「禁止 AI 代选模式」两处同义 → 合并
- SKILL L169「禁止用推荐直接写入 artifact_review_mode 或跳过询问」与 L218「禁止 AI 代选或跳过本步；用户选择优先于 3.3.1 推荐」同义 → 在 L218 保留唯一权威句，L169 改为引用。
- 预估削减：约 2 行。

### 类型 4 — 4.2 / 4.3 的 materials 列表近重复 → 引用
- 4.2 materials（L284 起，6 项）与 4.3 materials（L313 起，6 项）除 4.3 首项为 PrimaryReport 外几乎一致 → 4.3 改为「materials 同 4.2，首项替换为 PrimaryReport（D-2 下全文拼入）」。
- 预估削减：约 6 行。

### 类型 5 — 3.3.1 推荐判定信号表可压缩
- L171–189 两张信号表（推荐 A / 推荐 B，约 9 行说明）编码判断依据但措辞冗长 → 保留全部信号点，改为紧凑决策启发式（每条一行），不丢信号。
- 预估削减：约 10–15 行。

### 类型 6 — 续跑/恢复说明分散 → 集中一处
- L32、L54、L121、L372–376 四处讲「续跑/恢复」，触发点不同但可归集为单一「续跑」小节，用列表区分触发条件。
- 预估削减：约 5–8 行。

### 类型 7 — 强调性/解释性冗余句
- L229「完成条件见 policy §6…机械终检在 Step 4.1，不在本步 / 不在 policy」与 3.4 标题重复。
- L169/L218 同义（见类型 3）。
- L271 fallback 注记可收紧一句。
- 预估削减：约 3–5 行。

> **SKILL.md 预估再削减：约 35–45 行（379 → 约 335–345，约 9–12%）**，全部为「重述/重复/可合并」，不删任何闸口、路径、命令或输出规则。templates 两文件属输出契约，非冗余散文，不建议删减。

---

## 四、必须保留的核心要素（不可删）

1. **HARD-GATE 四条硬闸口**（intention 检查 / 生成 tasks 前必读 tasks-template / 必须走 policy / 禁用 `superpowers:using-git-worktrees`）— 仅类型 2 把其中两句改为「引用 policy」，语义不变。
2. **标识约定**全部路径与 `change_id` 主键定义 — 正确性基石。
3. **流程顺序 Step 0→5 及其阻塞条件**（零匹配阻断、intention 缺节阻断、tasks-lint 失败阻断等）。
4. **3.3 模式选择逻辑**：推荐判定 + 决策点 + 写入 `state.yaml` 的 `artifact_review_mode` — 仅压缩措辞，不删信号与「用户优先于推荐」原则。
5. **3.5 迁入命令与「禁止在 .polaris 保留副本」**（确定性 `mv` 命令与校验）— 原样保留。
6. **4.1 机械终检清单**（四件套存在性、proposal/design 必需节、`tasks-lint` 必跑）— 质量门禁核心。
7. **4.2 主审派发**（subagent-probe、`propose-reviewer`、materials 顺序、落盘路径）— 评审出口不可省（materials 仅去重）。
8. **4.4 消化规则**（BLOCK/APPROVE_WITH_CONCERNS、重跑上限 3 轮、动过四件套须先重跑 4.1）。
9. **policy §3/§4/§5 全部**（标准生成循环、反思循环 4a/4b/4c、review-log 结构、轮次计数、冻结/解冻）— 已优化但实质不变。
10. **templates/tasks-template.md 与 design-template.md 全部规则** — 输出契约。

---

## 五、精简方案（若进入写入确认）

| 步骤 | 文件 | 动作 | 预估削减 |
|---|---|---|---|
| 1 | SKILL | 3.4 执行范围表改为引用 3.3.2 | ~4 行 |
| 2 | SKILL | 4.2 模式语义压缩为引用 | ~2 行 |
| 3 | SKILL | HARD-GATE L14 / 4.2 L281 改为引用 policy 禁令 | ~3 行 |
| 4 | SKILL | 「禁止 AI 代选」两处合并为 L218 权威句 | ~2 行 |
| 5 | SKILL | 4.3 materials 改为引用 4.2 | ~6 行 |
| 6 | SKILL | 3.3.1 信号表压缩为紧凑启发式 | ~10–15 行 |
| 7 | SKILL | 续跑/恢复说明归集为单小节 | ~5–8 行 |
| 8 | SKILL | 删除强调性/解释性冗余句 | ~3–5 行 |
| 合计 | SKILL | — | **约 35–45 行** |

执行纪律：每步最小侵入、单点定义后引用、改完通读确认无断点、保留全部 HARD-GATE 与门禁原文。policy 文件本轮已完成，无需再动。

---

## 六、11 维评分（聚焦相关维度，附证据）

| # | 维度 | 评分 | 证据 |
|---|---|---|---|
| 9 | 参考资料质量（单一信息源/上下文成本） | **6** | policy 已去重（核心原则+§5.3 为权威）；但 SKILL.md 内 Mode 语义仍重述（3.3.2/3.4/4.2）、同类禁止约束 SKILL 与 policy 仍各写、materials 近重复 → 上下文成本仍偏高 |
| 5 | 工作流程完整性 | 9 | Step 0→5 无断点，阻塞/恢复/降级分支齐全 |
| 6 | 输出可执行性 | 9 | tasks-template 含模板+强制规则+lint；4.1 有可判定通过条件 |
| 7 | 边界与停止条件 | 9 | HARD-GATE、4c 人工决策、4.4 重跑上限、Mode B 禁跑 §4 均明确 |
| 8 | 复杂度适配 | 8 | 机制适配；冗余属「表述」非「机制过度」 |
| 11 | 安全与隐私 | 9 | 无硬编码密钥/外发/静默破坏；高风险操作均有确认与阻断 |
| 1–4,10 | 选题/定位/触发/增量/脚本 | 8–9 | 非本次焦点，均达标；本技能无包内脚本（依赖外部 hooks，N/A 不适用） |

**跨维度一致性**：维度 5/6/7 高分 ↔ 维度 9 低分——流程正确但「表述层」重复拉高上下文成本；精简不改变复杂度适配结论。

**综合判定**：五阶段结构/安全通过；无 P1；维度 9 < 8 → 落入「需修改（非阻断）」。policy 已完成优化；SKILL.md 按第五节可进一步降上下文成本。必要触发测试/脚本实测因本次为只读评估未执行，标注「未验证」。

---

## 七、其他发现（非篇幅问题，需核实，建议并入后续修复）

- **P2 — 断裂/错位引用**：SKILL.md L132 引用 `./templates/intention-template.md`，但该文件实际位于 `clarify/templates/intention-template.md`，propose 作用域内路径不存在。应改为正确跨技能路径或复制副本。
- **P2 — 缺失 policy 引用**：SKILL.md 引用 `./policies/decision-point.md`、`ask-question-react.md`、`outside-voice.md`、`auto-transition.md`，在 skills 树中均未找到（仅 `subagent-delegate-policy.md` 存在于 subagent-probe）。若由运行时 `PLUGIN_ROOT` 解析则无碍，否则为断裂引用，需确认真实位置并补全或修正路径。
- 上述两项不影响「篇幅冗余」结论，但属准确性缺陷，建议与 SKILL.md 精简同步处理。

---

## 八、影响评估（为何削减不影响效能/结构/质量）

- **执行效能**：精简仅删「重述与重复」。每个执行分支（Step 顺序、policy §3/§4/§5、hooks 调用）原文保留，模型读取路径不中断。
- **输出结构**：四件套路径、文件名、落盘顺序（proposal→specs→design→tasks）、review-log/reviews 目录均原样保留。
- **产物质量**：4.1 机械终检、`tasks-lint`、宪法对齐/备选方案/前提等必需节、冻结/解冻规则全部保留，质量门禁无削弱。
- **风险点**：去重后「引用」须可达——policy 内引用已验证可达（核心原则 L5 / §5.3 L263）；SKILL→policy 的引用需确保 policy 文件存在（已存在）。

---

*本报告为只读评估，未改动 `propose` 技能任何文件。policy 优化已于 2026-08-20 完成；如确认采纳第五节 SKILL.md 精简方案，请进入「写入确认」阶段，由我逐条备份并最小侵入修改。*
