# 产品需求文档（PRD）标准版模板
> 定位：企业级涉密业务系统功能模块需求说明书 | 支持Agent结构化填充与迭代维护
> 文档编号：`{{DOC_ID}}`
> 所属系统：`{{SYSTEM_NAME}}`
> 模块名称：`{{MODULE_NAME}}`
> 版本号：`{{VERSION}}`
> 编写人：`{{AUTHOR}}`
> 最后更新：`{{UPDATE_DATE}}`

### 版本修订记录
| 版本号 | 修订日期 | 修订人 | 修订内容 |
| :--- | :--- | :--- | :--- |
| {{V1.0}} | {{YYYY-MM-DD}} | {{姓名}} | 初始版本创建 |
| {{V1.1}} | {{YYYY-MM-DD}} | {{姓名}} | {{变更内容描述}} |

---

## 1 背景与目标
### 1.1 业务背景
【填写指引：描述业务场景现状、核心痛点、系统能力缺口，说明本模块建设的业务动因与合规风险，建议3-5条】
{{业务背景详细描述，包含行业合规要求、现有系统短板、业务管理风险点}}

### 1.2 核心目标
#### 功能目标
【填写指引：逐条列明可验证、可落地的功能建设目标，对应业务痛点】
1. {{功能目标1，如：实现主/子载体台账统一管理与多条件查询}}
2. {{功能目标2，如：支持载体全类型流转操作与全流程留痕追溯}}
3. {{功能目标3}}
4. ……

#### 质量目标
【填写指引：逐条列明数据一致性、准确性、可审计性等质量维度目标】
1. {{质量目标1，如：实物载体与电子台账事务强绑定，一一对应}}
2. {{质量目标2，如：主子载体状态联动零冲突、数据零错乱}}
3. {{质量目标3}}
4. ……

### 1.3 范围与边界
#### 包含范围
【填写指引：明确本模块覆盖的功能、数据、场景边界，颗粒度到功能点级】
1. {{包含范围1}}
2. {{包含范围2}}
3. {{包含范围3}}
4. ……

#### 不包含范围
【填写指引：明确交由外部模块实现、本期不建设、后续迭代扩展的能力边界，避免需求蔓延】
1. {{不包含范围1，如：载体号生成规则由独立编号模块实现}}
2. {{不包含范围2，如：业务审批流程由外部工作流引擎提供}}
3. {{不包含范围3}}
4. ……

---

## 2 术语定义
【填写指引：统一定义业务专有名词、状态枚举、缩写词，确保产研测三方理解一致；状态类枚举需明确中间态/终态属性】

| 术语 | 类型 | 说明 |
| :--- | :--- | :--- |
| {{术语1}} | 业务名词/状态枚举 | {{术语详细释义、枚举值说明}} |
| {{术语2}} | 业务名词/状态枚举 | {{术语详细释义、枚举值说明}} |
| {{术语3}} | 业务名词/状态枚举 | {{术语详细释义、枚举值说明}} |
| …… | …… | …… |

---

## 3 业务角色与端到端流程
### 3.1 角色与权限说明
【填写指引：明确本模块涉及的所有用户角色、对应职责与操作权限范围】
| 角色名称 | 角色标识 | 职责说明 | 核心操作权限 |
| :--- | :--- | :--- | :--- |
| {{角色1，如：普通用户}} | {{USER}} | {{职责描述}} | {{操作权限，如：发起载体流转、查看本人载体台账}} |
| {{角色2，如：载体管理员}} | {{ADMIN}} | {{职责描述}} | {{操作权限，如：全量台账查询、载体归档销毁}} |
| {{角色3，如：审计员}} | {{AUDITOR}} | {{职责描述}} | {{操作权限，如：生命周期追溯、审计记录查询}} |
| …… | …… | …… | …… |

### 3.2 业务总流程
【填写指引：描述本模块覆盖的端到端业务主流程，建议配合流程图；按阶段说明流转节点】
{{端到端业务流程文字描述，例如：载体生成→在用管理→流转操作（回收/销毁/归档/外发/归属变更）→终态封存，全流程生命周期留痕}}

### 3.3 核心业务场景用例
【填写指引：列举最高频的核心业务场景，说明角色、触发条件、预期结果，建立整体业务认知】
1. **场景1：{{场景名称，如：载体打印生成}}**
   - 触发角色：{{角色}}
   - 触发条件：{{触发前提}}
   - 预期结果：{{业务产出与系统行为}}
2. **场景2：{{场景名称，如：部分载体回收审批}}**
   - 触发角色：{{角色}}
   - 触发条件：{{触发前提}}
   - 预期结果：{{业务产出与系统行为}}
3. **场景3：{{场景名称，如：载体生命周期追溯审计}}**
   - 触发角色：{{角色}}
   - 触发条件：{{触发前提}}
   - 预期结果：{{业务产出与系统行为}}
4. ……

---

## 4 核心领域数据模型
### 4.1 实体关系总览
【填写指引：描述核心实体之间的关联关系、关联字段、映射基数，建立数据整体骨架】
{{实体关系说明，例如：
- 主载体表与子载体表：一对多关系，通过 CARRIER_NO 外键关联；一个主载体对应N条子载体（纸张按页数、光盘固定1条）
- 主载体表与生命周期记录表：一对多关系，通过 CARRIER_NO 外键关联；一条主载体对应多条生命周期变更记录}}

### 4.2 {{主实体名称，如：主载体表 CARRIER_MAIN}}
【填写指引：实体业务定位，如：主台账核心表，记录载体聚合属性、状态与归属信息】

| 字段名 | 数据类型 | 必填 | 业务规则说明 |
| :--- | :--- | :--- | :--- |
| {{字段名1}} | {{BigInt/String/Enum}} | {{Y/N}} | {{字段约束、生成规则、可修改性、枚举值说明}} |
| {{字段名2}} | {{BigInt/String/Enum}} | {{Y/N}} | {{字段约束、生成规则、可修改性、枚举值说明}} |
| {{字段名3}} | {{BigInt/String/Enum}} | {{Y/N}} | {{字段约束、生成规则、可修改性、枚举值说明}} |
| …… | …… | …… | …… |

### 4.3 {{子实体名称，如：子载体表 CARRIER_CHILD}}
【填写指引：实体业务定位，如：分页明细表，与主实体一对多关联，记录细粒度单页状态】

| 字段名 | 数据类型 | 必填 | 业务规则说明 |
| :--- | :--- | :--- | :--- |
| {{字段名1}} | {{BigInt/String/Enum}} | {{Y/N}} | {{字段约束、生成规则、可修改性、枚举值说明}} |
| {{字段名2}} | {{BigInt/String/Enum}} | {{Y/N}} | {{字段约束、生成规则、可修改性、枚举值说明}} |
| {{字段名3}} | {{BigInt/String/Enum}} | {{Y/N}} | {{字段约束、生成规则、可修改性、枚举值说明}} |
| …… | …… | …… | …… |

### 4.4 {{流水/审计实体名称，如：载体生命周期表 CARRIER_LIFECYCLE}}
【填写指引：实体业务定位，如：审计留痕表，记录所有状态、归属、操作变更轨迹】

| 字段名 | 数据类型 | 必填 | 业务规则说明 |
| :--- | :--- | :--- | :--- |
| {{字段名1}} | {{BigInt/String/Enum}} | {{Y/N}} | {{字段约束、生成规则、可修改性、枚举值说明}} |
| {{字段名2}} | {{BigInt/String/Enum}} | {{Y/N}} | {{字段约束、生成规则、可修改性、枚举值说明}} |
| {{字段名3}} | {{BigInt/String/Enum}} | {{Y/N}} | {{字段约束、生成规则、可修改性、枚举值说明}} |
| …… | …… | …… | …… |

---

## 5 功能需求详述
【填写指引：按「模块→功能点」分级拆解；通用流程与公共约束前置提炼，避免重复描述；每个功能点明确前置条件、业务逻辑、约束规则、UI交互、验收标准】

### 5.1 {{功能模块编号}} {{功能模块名称，如：F001 载体台账管理}}
【模块概述：简述本模块核心能力与业务价值】

#### 5.1.1 {{功能点编号}} {{功能点名称，如：F001-001 载体新增接口}}
**前置条件**
1. {{前置条件1，如：外部模块传入合法且唯一的载体编号}}
2. {{前置条件2，如：调用方拥有对应操作权限}}
3. ……

**业务逻辑**
【填写指引：按执行顺序描述处理流程，明确事务边界、数据落库规则、批量生成规则】
1. {{业务步骤1，如：校验载体号唯一性与必填字段完整性}}
2. {{业务步骤2，如：保存主载体基础数据}}
3. {{业务步骤3，如：按载体类型批量生成子载体}}
4. {{业务步骤4，如：事务内同步写入第一条生命周期记录}}
5. ……

**约束规则**
1. {{约束1，如：载体号、密级、载体名称创建后永久不可修改}}
2. {{约束2，如：多副本独立生成主载体，副本间无关联}}
3. ……

**UI交互说明（页面类功能填写）**
1. 查询栏：{{支持的查询条件列表}}
2. 列表展示字段：{{字段名称列表，关键字段标注展示规则}}
3. 操作栏：{{操作按钮名称 + 交互跳转说明}}
4. 分页排序：{{默认分页条数、排序字段与规则}}

**验收标准**
1. {{验收点1，如：传入合法参数时，主载体、子载体、生命周期记录在同一事务中同时落库}}
2. {{验收点2，如：传入重复载体号时，返回唯一性校验错误，无数据写入}}
3. {{验收点3，如：纸质载体按总页数生成对应数量子载体，状态均为「在用」}}
4. ……

#### 5.1.2 {{功能点编号}} {{功能点名称}}
……

### 5.2 {{功能模块编号}} {{功能模块名称，如：F002 载体变动操作}}
【模块概述：简述本模块核心能力与业务价值】

#### 通用处理流程
【填写指引：本模块所有操作遵循的统一执行顺序，明确事务原子性要求，所有子功能必须遵守】
1. {{步骤1：更新操作范围内子载体的状态/归属信息}}
2. {{步骤2：更新主载体统计字段与归属字段}}
3. {{步骤3：执行主载体聚合状态判定算法，更新主载体状态}}
4. {{步骤4：写入载体生命周期变更记录}}

#### 前置公共约束
【填写指引：本模块所有操作必须遵守的准入规则与通用限制，拦截非法操作】
1. {{约束1，如：仅主载体状态为「在用」「部分在用」时，允许发起流转流程}}
2. {{约束2，如：所有流转操作走外部审批引擎，审批结果异步回写}}
3. {{约束3，如：审批驳回时，本次操作涉及的子载体全部恢复为「在用」状态}}
4. ……

#### 核心算法：{{算法名称，如：主载体聚合状态判定算法}}
- **输入**：{{输入参数与数据来源说明，如：主载体下全部子载体集合、本次操作页码范围}}
- **输出**：{{输出结果与作用说明，如：主载体聚合状态 CARRIER_STATUS}}
- **判定逻辑**：
  1. {{条件分支1，如：存在在用子载体时，若混杂其他状态则为「部分在用」，全部在用则为「在用」}}
  2. {{条件分支2，如：无在用子载体时，按局部/全量操作分别判定}}
  3. {{条件分支3}}
  4. ……

#### 特殊对象规则
【填写指引：特殊类型载体/对象的差异化处理规则】
1. {{规则1，如：光盘载体仅1条子载体，无局部操作，所有流转等价全量操作}}
2. {{规则2}}
3. ……

---

## 6 核心业务规则汇总
【填写指引：提炼跨功能模块的全局业务规则，作为开发、测试、验收的统一基准；规则必须唯一、无矛盾、可落地】

### 6.1 载体生成规则
1. {{规则条目1，如：实物打印/刻录发起时同步创建电子台账，实物与台账一一对应}}
2. {{规则条目2，如：多份打印每份生成独立主载体，互不关联}}
3. ……

### 6.2 状态聚合判定规则
1. {{规则条目1，如：主载体状态由子载体状态自动聚合计算，不硬编码场景分支}}
2. {{规则条目2}}
3. ……

### 6.3 操作限制规则
1. {{规则条目1，如：终态载体、全部办结载体禁止发起任何流转操作}}
2. {{规则条目2，如：载体密级、编号、名称创建后永久不可修改}}
3. ……

### 6.4 生命周期留痕规则
1. {{规则条目1，如：载体新建、审批通过、审批驳回均单独生成记录，不覆盖历史}}
2. {{规则条目2，如：所有生命周期记录永久存储，禁止编辑、删除}}
3. ……

### 6.5 数据隔离与删除规则
1. {{规则条目1，如：主/子载体、生命周期记录均不提供物理删除接口}}
2. {{规则条目2，如：载体作废通过状态流转至终态实现业务隔离，审计数据永久留存}}
3. ……

---

## 7 状态流转规范
【填写指引：明确全量状态枚举、流转触发条件、中间态/终态定义、联动逻辑；强状态机系统核心章节】

### 7.1 状态枚举定义
| 中文状态 | 枚举Code | 状态类型 | 释义说明 |
| :--- | :--- | :--- | :--- |
| {{状态1，如：在用}} | {{IN_USE}} | 正常态 | {{状态业务含义}} |
| {{状态2，如：回收中}} | {{RECYCLING}} | 中间态 | {{状态业务含义，可驳回回退}} |
| {{状态3，如：已回收}} | {{RECYCLED}} | 终态 | {{状态业务含义，不可回退}} |
| …… | …… | …… | …… |

### 7.2 流转总原则
1. {{原则1，如：终态不可回退，所有终态操作不可逆}}
2. {{原则2，如：聚合状态严格依据算法自动计算，不人工干预}}
3. {{原则3，如：中间态流程驳回后，统一回退至操作前状态}}
4. ……

### 7.3 典型场景流转规则
【填写指引：按业务场景补充流转逻辑，供测试场景设计参考，不作为编码直接依据】
1. **场景：全量回收**
   - 初始状态：在用
   - 流程发起：全部子载体变为回收中 → 主载体变为回收中
   - 审批通过：全部子载体变为已回收 → 主载体变为已回收
   - 审批驳回：全部子载体恢复在用 → 主载体恢复在用
2. **场景：部分销毁**
   - 初始状态：在用
   - 流程发起：选中子载体变为销毁中 → 主载体变为部分在用
   - 审批通过：选中子载体变为已销毁 → 主载体按剩余子载体状态重新聚合
   - 审批驳回：选中子载体恢复在用 → 主载体恢复在用
3. ……

---

## 8 非功能需求
### 8.1 性能需求
【填写指引：尽量量化，明确接口响应耗时、批量处理性能、索引设计要求】
1. {{性能指标1，如：单载体查询响应时间 < 1秒}}
2. {{性能指标2，如：单条载体新增操作耗时 < 2秒}}
3. {{性能指标3，如：批量更新子载体采用批量SQL优化，支持百级子载体秒级更新}}
4. 数据库索引设计：{{建议建立的索引列表，如：生命周期表建立载体号+操作时间联合索引}}

### 8.2 安全需求
【填写指引：贴合涉密系统要求，明确数据加密、防篡改、权限控制、审计要求】
1. {{安全要求1，如：涉密数据传输链路全程加密}}
2. {{安全要求2，如：生命周期记录仅支持查询，禁止修改、删除，保证审计不可篡改}}
3. {{安全要求3，如：不提供物理删除接口，历史数据永久留存用于审计核查}}
4. {{安全要求4，如：操作权限与数据范围按角色分级控制}}

### 8.3 可扩展性需求
1. {{扩展性要求1，如：载体类型支持后续扩展，无需改动核心流程}}
2. {{扩展性要求2，如：支持后续批量导入导出功能迭代}}
3. ……

### 8.4 兼容性需求（可选）
{{兼容性要求，如：适配主流浏览器、兼容现有系统接口规范}}

---

## 9 验收标准
【填写指引：明确本模块上线验收的通过准则，分维度量化，作为交付验收依据】

### 9.1 功能完整性验收
1. {{验收点1，如：包含范围中所有功能点全部实现，无遗漏}}
2. {{验收点2，如：所有前置公共约束生效，非法操作均被拦截并返回正确提示}}
3. {{验收点3，如：主子载体状态联动符合聚合算法，无状态冲突}}

### 9.2 数据一致性验收
1. {{验收点1，如：主载体统计字段与子载体实际状态实时一致，无数据偏差}}
2. {{验收点2，如：所有状态、归属变更均生成对应生命周期记录，无遗漏、无重复}}
3. {{验收点3，如：事务操作失败时整体回滚，无部分落库数据}}

### 9.3 性能验收
1. {{验收点1，如：单条查询响应时间达标率100%}}
2. {{验收点2，如：并发量XX时，接口成功率≥99.9%}}
3. ……

### 9.4 安全与合规验收
1. {{验收点1，如：无物理删除接口，所有变更可追溯}}
2. {{验收点2，如：权限控制生效，越权操作全部拦截}}
3. ……

---

## 10 待确认事项
【填写指引：列出当前未闭环、需业务/技术侧确认的问题；全部闭环则填「无」】
1. {{待确认事项1}}
2. {{待确认事项2}}
3. ……

---

## 附录
### 附录A：业务场景枚举
【填写指引：补充全量业务场景用例，供产品、测试人员验证参考，不直接作为编码依据】
- 场景1：{{场景描述 + 处理逻辑说明}}
- 场景2：{{场景描述 + 处理逻辑说明}}
- 场景3：{{场景描述 + 处理逻辑说明}}
- ……

### 附录B：参考资料
- {{参考文档/系统/接口名称与链接}}
- ……