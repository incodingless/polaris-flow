# 原型交付评审报告

> **使用说明**：本模板是 `polaris{{SKN_SPR}}prototype{{SKN_SPR}}review` 技能 Step 6 的标准输出格式。填充所有 `{占位符}`；**没有内容的区块保留标题并标注「无」**，不得整节删除——缺节会让结论看起来比实际更完整。
> 判据出处一律写 `§33` / `§34` / `§36`（唯一来源：本技能 `references/01-quality-criteria.md`）与 `§27.1`（三层验证规格，见 `polaris{{SKN_SPR}}prototype{{SKN_SPR}}build` → `references/04`），本报告不复制判据原文。
> 文件命名建议：`原型评审报告_<原型文件名>_<YYYYMMDD>.md`

---

## 一、评审对象与范围

| 项 | 值 |
|---|---|
| 原型文件 | `{路径}` |
| 字节数 | `{N}` |
| sha256 | `{hash}` |
| 评审日期 | `{YYYY-MM-DD}` |
| 评审范围 | `{全量 \| 指定页面（列出未覆盖页面）}` |
| 评审用途 | `{交付前把关 \| 研发交接判定 \| 独立复核}` |
| 输入材料 | 黄金流：`{有 / 无}`　需求文档：`{有 / 无}`　设计规范：`{有 / 无}`　研发交接说明：`{有 / 无}` |
| 只评不改 | 已声明并执行：评审前后原型 sha256 **一致** ✅ / ⚠️ `{不一致 → 必须说明原因并停止}` |

**未覆盖项**：`{本次未评审的页面 / 维度 / 层；无则写「无」}`

---

## 二、结论

**`{可交付 | 修复后可交付 | 不得交付}`**

依据：P0 `{N}` 项 · P1 `{N}` 项 · P2 `{N}` 项 · P3 `{N}` 项；三层验证 `{L1/L2/L3 各自结论}`。
一句话说明：`{为什么是这个结论}`

> 存在任一 P0 → 结论一律「不得交付」。

---

## 三、三层可执行验证结果

执行命令：`node scripts/verify.mjs {原型文件} --flow={flow.json} --strict`
退出码：`{0 | 1 | 2 | 3}`

| 层 | 内容 | 结果 | HARD | WARN | 备注 |
|---|---|---|---|---|---|
| L1 | 静态检查 | `{PASS / FAIL / 未执行}` | `{}` | `{}` | |
| L2 | 无头浏览器逐页冒烟 | `{}` | `{}` | `{}` | |
| L3 | 黄金流 + 三档分辨率 | `{}` | `{}` | `{}` | |

- 未执行层及原因：`{无 / 原因}`（退出码 3 = `--strict` 下必需层被跳过，按**未完成**计）
- HARD 命中项：`{逐条列出检查项 ID + 原文结论；无则写「无」}`
- WARN 项的人工判读：`{逐条写明判为缺陷还是可接受；无则写「无」}`

---

## 四、问题分布矩阵

| 维度 | P0 | P1 | P2 | P3 | 合计 |
|---|:--:|:--:|:--:|:--:|:--:|
| BIZ 业务 | | | | | |
| IA 结构 | | | | | |
| IX 交互 | | | | | |
| VIS 视觉 | | | | | |
| DEV 研发 | | | | | |
| VETO 一票否决 | | | | | |
| ENV 可执行验证 | | | | | |
| **合计** | | | | | |

---

## 五、按维度问题明细（检查过程视图）

> 每条问题的「问题ID」需在此处建立锚点：`<a id="biz-001">BIZ-001</a>`，供第六节回链。
> 无问题的维度**保留标题并写「无」**——「查过了没问题」和「没查」必须看得出区别。

### BIZ 业务（`§33.1`）

| 问题ID | 问题标题 | 定位证据 | 等级 | 整改建议 |
|---|---|---|---|---|
| | | | | |

### IA 结构（`§33.2`）

| 问题ID | 问题标题 | 定位证据 | 等级 | 整改建议 |
|---|---|---|---|---|
| | | | | |

### IX 交互（`§33.3`）

| 问题ID | 问题标题 | 定位证据 | 等级 | 整改建议 |
|---|---|---|---|---|
| | | | | |

### VIS 视觉（`§33.4`）

| 问题ID | 问题标题 | 定位证据 | 等级 | 整改建议 |
|---|---|---|---|---|
| | | | | |

### DEV 研发（`§33.5`）

| 问题ID | 问题标题 | 定位证据 | 等级 | 整改建议 |
|---|---|---|---|---|
| | | | | |

### 演示就绪（`§33.6`，演示 / 比赛用途时必做）

| 问题ID | 问题标题 | 定位证据 | 等级 | 整改建议 |
|---|---|---|---|---|
| | | | | |

### VETO 一票否决核对（`§34` 全 31 条）

| 问题ID | 命中条款 | 定位证据 | 整改建议 |
|---|---|---|---|
| | `§34 第 N 条` | | |

---

## 六、分级问题清单（处理决策视图）

> 与第五节**同一批问题**、ID 一一对应，两处数量必须一致。问题ID 输出为链接：`[BIZ-001](#biz-001)`。

### P0 阻断（必须修复后重评审）

| 问题ID | 维度 | 问题标题 | 定位证据 | 整改建议 |
|---|---|---|---|---|
| | | | | |

### P1 重要（交研发前必修）

| 问题ID | 维度 | 问题标题 | 定位证据 | 整改建议 |
|---|---|---|---|---|
| | | | | |

### P2 一般（可随迭代修）

| 问题ID | 维度 | 问题标题 | 定位证据 | 整改建议 |
|---|---|---|---|---|
| | | | | |

### P3 待确认（需产品经理决策，**不作为缺陷**）

| 问题ID | 待确认事项 | 定位依据 | 影响范围 | 建议决策人 | 需在何时确认 |
|---|---|---|---|---|---|
| | | | | | |

---

## 七、证据不足与未执行项

> 本节内容**不计入问题清单**，也**不得**作为「通过」的依据。它回答的是「哪些结论我给不了，以及为什么」。

| # | 项 | 缺什么证据 | 影响哪个维度 / 层 | 建议补充方式 |
|---|---|---|---|---|
| | | | | |

---

## 八、重评审记录

| 轮次 | 日期 | 原型 sha256 | 上轮结论 | 本轮结论 | 关闭项 | 新增项 |
|---|---|---|---|---|---|---|
| 1 | | | — | | — | |
