# {{change-name}} - 数据模型设计

> 存储形状：表/集合、字段、索引、引用完整性、迁移。禁止 HTTP 契约、仓储接口、领域行为重写。  
> 清单来源：优先 `repository-design.md` §7；字段含义对齐 `domain-model-design.md`。

## 0. 范围说明（可选）

| 范围内 | 范围外 |
|--------|--------|
| {{.Content}} | {{.Content}} |

## 1. 存储约定与映射

### 1.1 存储约定

| 项 | 约定 |
|----|------|
| 存储技术 | {MySQL / PostgreSQL / MongoDB / 其他；依据 detailed-design；未定须标假设} |
| 表名 / 字段 | {复数 + snake_case / 项目已有约定} |
| 主键策略 | {BIGINT 自增 / UUID / 雪花 / 其他；全篇一致} |
| 时间戳字段 | {默认 `created_at` / `updated_at`；若项目用 `create_time` 则全篇用项目名} |
| 物理外键 | {使用 / 不使用；不使用则引用完整性走应用层，见 §4} |

### 1.2 表 ↔ 聚合 / 仓储映射

| 表名 | 对应聚合根 / 仓储 | 持久化角色 | 备注 |
|------|-------------------|------------|------|
| {复数表名} | {与 repository/domain-model 同名或 -} | 根表 / 子实体表 / 值对象展开 / 读模型 / 技术表 | |

> 写模型根表必须能指回仓储持久化单元（若有 repository）。技术表不得填业务聚合名充数。

## 2. ER 关系图

```mermaid
erDiagram
    {{.Content}}
```

> 须标 1:1 / 1:n / n:n；n:n 画出中间表。实体名与 §1.2 / §3 表名一致。

## 3. 表结构设计

**每个表必须独立成章**，章名：`3.x {表名}`。§1.2 与 ER 中出现的表不可缺章。

### 3.1 {table_name}

#### 3.1.1 业务说明

<!-- 存什么、服务哪条业务流程、对应哪个聚合/仓储；技术表须写明用途。 -->

#### 3.1.2 表结构定义

| 字段名 | 类型 | 是否必填 | 默认值 | 业务含义 | 约束/校验规则 |
|--------|------|----------|--------|----------|----------------|
| id | {按 §1.1 主键策略} | 是 | | 主键 | PRIMARY KEY |
| | | | | | |
| created_at | DATETIME | 是 | CURRENT_TIMESTAMP | 创建时间 | |
| updated_at | DATETIME | 是 | CURRENT_TIMESTAMP | 更新时间 | |

#### 3.1.3 索引设计

| 索引名称 | 索引类型 | 字段 | 业务场景 |
|----------|----------|------|----------|
| | 普通 / 唯一 / 联合 | | {对应哪条热点查询或唯一约束} |

#### 3.1.4 数据量与性能评估

| 指标 | 预估值 | 说明 |
|------|--------|------|
| 预期日增量 | | 有依据写数字；否则量级 +「假设」 |
| 预期总数据量 | | 如一年后 |
| 查询频率 | | QPS 或量级 |
| 热点查询 | | 高频 WHERE / ORDER BY |

### 3.2 {table_name}

……

## 4. 引用完整性

| 从表 | 外键字段 | 主表 | 主键字段 | 物理/逻辑 | 删除策略 | 更新策略 | 业务含义 |
|------|----------|------|----------|-----------|----------|----------|----------|
| | | | | 物理 FK / 逻辑引用 | RESTRICT / SET NULL / CASCADE / 应用层 | | |

> 删除策略须能讲清为什么；禁止无论证一律 CASCADE。

## 5. 数据迁移方案

### 5.1 建表 / 变更 DDL

```sql
-- 完整可执行 DDL；禁止省略关键列
```

### 5.2 回滚 DDL

```sql
-- DROP / 回滚变更；须可逆或写明不可逆及理由
```

### 5.3 数据迁移说明

| 项 | 说明 |
|----|------|
| 是否有旧表 | 有 / 无（绿场仅建表则其余列填「不适用」） |
| 字段映射 | {旧列 → 新列} |
| 转换规则 | {类型、默认值、拆分/合并} |
| 迁移顺序 | {有 FK 时的表顺序} |
| 回填 / 校验 | {如何确认行数与关键不变量} |
