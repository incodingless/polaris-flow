# {{change-name}} - 仓储设计文档

> 抽象存储层：行为与结构契约。禁止 SQL / ORM / DDL / 框架代码。  
> 领域概念唯一来源：`domain-model-design.md`。

## 0. 范围说明（可选）

| 范围内 | 范围外 |
|--------|--------|
| {{.Content}} | {{.Content}} |

## 1. 仓储概览

### 1.1 核心职责

| 职责项 | 说明 |
|--------|------|
| 聚合存取 | 对【核心领域/业务场景】聚合根的存储、查询、更新、删除抽象 |
| 边界封装 | 封装领域模型与持久化层的交互边界 |
| 事件协作 | 领域事件触发后的存储行为（与 domain-model 对齐） |
| 一致性 | 保障**单聚合**内数据一致性 |

### 1.2 仓储边界

| 类型 | 内容 |
|------|------|
| 负责范围 | {本专项管理的聚合根；内部实体/值对象经根间接存取} |
| 不负责范围 | {他聚合写入；跨聚合上帝仓储；读模型/报表；具体库表实现} |

### 1.3 领域模型关联

| 项 | 内容 |
|----|------|
| 依据文档 | `domain-model-design.md`（必填） |
| 核心聚合根 | {与 domain-model 一致的名称列表} |
| 关联实体 | {列表或 -} |
| 关联值对象 | {列表或 -} |
| 关联领域服务 | {列表或 -} |
| 关联领域事件 | {列表或 -} |

## 2. 仓储结构设计（强制）

| 仓储名称 | 对应领域聚合根 | 仓储描述 | 核心操作 | 关联仓储 | 领域事件关联 |
| -------- | -------------- | -------- | -------- | -------- | ------------ |
| {PascalCase，如 OrderRepository} | {与 domain-model 同名} | {≤200 字} | save、findById、findByCondition、delete、… | {依赖说明或 -} | {事件 → 仓储行为；或 -} |
| {{.Content}} | | | | | |

约束：一行一聚合根；禁止 `DataRepository` / `CommonRepository` 等无领域语义命名。

## 3. 仓储行为契约明细

> 每个仓储一小节：`### 3.x {RepositoryName}`。只写领域语义契约，不写实现。

### 3.1 {RepositoryName}

#### 3.1.1 方法清单

| 方法 | 意图 | 入参（领域语义） | 返回 | 前置条件 / 不变式 | 场景依据（domain-model） |
| ---- | ---- | ---------------- | ---- | ----------------- | ------------------------ |
| {{.Content}} | | | | | |

#### 3.1.2 加载与保存策略（抽象）

| 策略项 | 说明 |
|--------|------|
| 加载范围 | {完整聚合 / 其他；须领域可审} |
| 保存语义 | {新建与更新如何区分；不写 upsert SQL} |
| 删除语义 | {硬删 / 软删 / 领域终止；依据 domain-model} |

## 4. 事务与工作单元（抽象）

| 场景 | 事务边界 | 参与仓储 | 失败策略 |
| ---- | -------- | -------- | -------- |
| 单命令单聚合 | 默认单事务 | {仓储} | 回滚 |
| 跨聚合 | {最终一致 / 禁止强一致同库写多聚合 / 其他已论证方案} | {列表} | {{.Content}} |

| 项 | 说明 |
|----|------|
| 工作单元 | {采用 / 不采用及原因；不写框架 API} |

## 5. 查询与读模型

| 查询需求 | 归属（写模型仓储 / 读模型端口） | 与领域场景关系 |
| -------- | ------------------------------- | -------------- |
| {{.Content}} | | |

## 6. 并发与幂等（抽象）

| 机制 | 适用范围 | 冲突 / 重复时的领域行为 |
| ---- | -------- | ----------------------- |
| {{.Content}} | | |

## 7. 与持久化映射（给 data-model）

| 聚合根 / 仓储 | 预期持久化单元（表/集合/文档名级） | 备注 |
| ------------- | ---------------------------------- | ---- |
| {{.Content}} | | |

> 只到「单元」级；字段、索引、DDL、迁移见 `data-model-design.md`。

## 8. 风险与开放问题

| 风险 / 问题 | 影响 | 缓解或待决 |
| ----------- | ---- | ---------- |
| {{.Content}} | | |
