# {{change-name}} - 数据库设计

## 1. ER 关系图

```mermaid
erDiagram
    {{.Content}}
```

## 2. 表结构设计

**每个表必须独立成章**，章节命名规范：`2.x {表名}`

### 2.1 <table_name>

#### 2.1.1 业务说明

<!-- 清晰阐述此表的业务用途、存储的核心数据、与业务流程的关系。 -->

#### 2.1.2 表结构定义

| 字段名 | 类型 | 是否必填 | 默认值 | 业务含义 | 约束/校验规则 |
| ------ | ---- | -------- | ------ | -------- | ------------ |
| id | BIGINT | 是 | 自增 | 主键 | PRIMARY KEY, AUTO_INCREMENT |
| | | | | | |
| create_time | DATETIME | 是 | CURRENT_TIMESTAMP | 创建时间 | |
| update_time | DATETIME | 是 | CURRENT_TIMESTAMP ON UPDATE | 更新时间 | |

#### 2.1.3 索引设计

| 索引名称 | 索引类型 | 字段 | 业务场景 |
| -------- | -------- | ---- | -------- |
| | | | |

#### 2.1.4 数据量与性能评估

| 指标 | 预估值 | 说明 |
| ---- | ------ | ---- |
| 预期日增量 | | 每日新增行数 |
| 预期总数据量 | | 一年后的预估总量 |
| 查询频率 | | 每秒查询次数（QPS） |
| 热点查询 | | 高频查询的 WHERE/ORDER BY 条件 |

### 2.2 <table_name>
......

## 3. 外键关系与引用完整性

| 从表 | 外键字段 | 主表 | 主键字段 | 删除策略 | 更新策略 | 业务含义 |
| ---- | -------- | ---- | -------- | -------- | -------- | -------- |
| | | | | | | |

## 4. 数据迁移方案

### 4.1 迁移脚本

```sql
-- 建表 DDL
```

### 4.2 回滚脚本

```sql
-- 回滚 DDL
```

### 4.3 数据迁移说明

<!-- 描述从旧表到新表的数据迁移逻辑，包括字段映射、数据转换规则、迁移顺序等。 -->
