# {{change-name}} - 仓储设计文档

## 1. 仓储概览

### 1.1 核心职责
- 负责【核心领域/业务场景】的聚合根数据存储、查询、更新、删除抽象
- 封装领域模型与持久化层的交互边界
- 响应领域事件触发的存储行为
- 保障领域数据的一致性与完整性

### 1.2 仓储边界
- **负责范围**：{明确本仓储管理的聚合根、实体、值对象}
- **不负责范围**：{明确排除的存储职责，避免跨领域/跨聚合根污染}

### 1.3 领域模型关联
- 核心聚合根：{对应design-domain-model.md中的聚合根名称}
- 关联实体：{关联的领域实体列表}
- 关联值对象：{关联的领域值对象列表}
- 关联领域服务：{关联的领域服务列表}

## 2. 仓储结构设计（Repository Structure）
| 仓储名称 | 对应领域聚合根 | 仓储描述 | 核心操作 | 关联仓储 | 领域事件关联 |
| -------- | -------------- | -------- | -------- | -------- | ------------ |
| {PascalCase命名，如UserRepository} | {聚合根名称} | {200字内核心职责描述} | save、findById、findByXXX、delete、batchSave等 | {依赖/关联的仓储名称，无则填-} | {关联的领域事件及触发行为，无则填-} |
{{.Content}}