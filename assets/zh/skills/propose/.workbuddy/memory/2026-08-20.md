# 2026-08-20 工作日志

## 优化 propose 技能（措辞精简）

- 先对 `propose` 技能做只读冗余评估（产出 `polaris-flow/propose-skill-conciseness-review.md`）：判定「需修改（非阻断）」，主因 Mode A/B 语义跨两文件重述约 40 次、同类禁令重复、流程图重复。
- 用户确认后，按写入确认方案仅优化 `policies/artifact-batch-generation.md`：
  - 备份：`policies/artifact-batch-generation.md.bak.2026-08-20`（297 行，已验证可读）
  - 改动：§1 模式→路径表改为引用核心原则；§3.2 模式分支改为引用核心原则；§4.6 重复流程图改为指向 §5.3；§6 收尾句轻量收紧。
  - 结果：297 → 286 行（削减 11 行）；`核心原则`(L5) 与 `§5.3`(L263) 引用均可达；HARD 门禁、`tasks-lint`(在 SKILL.md)、冻结/解冻、review-log 等全部保留。
- 未改动 SKILL.md 与 templates；评估中发现的 `intention-template.md` 路径错位及若干 policy 引用缺失（P2）尚未处理，留待后续。
