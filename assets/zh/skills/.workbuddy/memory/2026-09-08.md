# 2026-09-08

## coding/clarify → coding/specify 全链路改名

用户决策（AskUserQuestion 确认）：
- **深度**：全链路一致改名（技能目录/技能名/链路标签/phase 枚举与初始 phase/runtime 键/TS 类型/finalize 脚本/测试夹具全部 clarify→specify）
- **兼容**：预发布无需兼容旧状态文件（不加 runtime.clarify→specify 迁移）
- **中文名**：保留「澄清/澄清需求」，只动英文 token（SKILL 标题、状态行、README 中文标签均未改）

改动范围（54 文件，tests 8 文件 67 例全绿，tsc --noEmit 通过）：
- 目录：`assets/zh/skills/coding/clarify/` → `coding/specify/`（git mv，5 文件）；`assets/shared/scripts/clarify-finalize.sh` → `specify-finalize.sh`
- src：`task-state.ts`（`TaskRuntimeClarifyState`→`TaskRuntimeSpecifyState`、`runtime.clarify`→`runtime.specify`、默认块、migrateLegacyShape 目标键）、`hooks/task.ts`（init/finalize）、`task-kind-layout.ts initialPhase`、`polaris-project-config.ts` phase 枚举、`intention-validate.ts`、`cli/index.ts`、注释示例（layout/install/skills.ts）、`scripts/check_state_yaml_three_way.py`
- 共享：state.example.yaml、workflow-template.yaml、scripts/README.md
- 技能文档：specify 自身 + build/design/normal/plan/propose/retro/ship/tweak/verify 的链路标签与阶段引用、skills/README.md、commands/polaris-flow.md、zh/policies/context-recovery.md、docs 两篇、README-zh
- 顺带修正：specify/SKILL.md Step 1「重新调用 clarify-init.sh」笔误（该脚本本不存在）→ `task-init.sh`

有意保留的 clarify 残留（勿再动）：
- `task-state.ts` migrateLegacyShape 读取旧扁平结构的 source 键（parsed.clarify / legacyClarify）与描述旧结构的注释
- `test/ts/task-state.test.ts` 旧扁平夹具输入（phase: clarify / 顶层 clarify:）与第 9 行注释
- PRD 域《需求澄清纪要》`req_clarify_summary.md` / `clarify_summary_id`（prd-discovery/draft/README、prd-state.example.yaml、requirements-engineering）
- assets/tmp/（state-yaml 审计、explore-router、idea-discovery 原型）、CHANGELOG.md、propose-skill-conciseness-review.md（历史快照）、test/fixtures/explore-router（孤儿夹具）、.cursor/ 第三方英文散文
- dist/ 为未入库产物（git ls-files 为空），未手改，重新 build 即更新

操作提示：repo 存在 09-05 残留的 .git/index.lock（陈旧），git mv/add 前需先 `rm -f .git/index.lock`。

## coding/plan → coding/tasks 全链路改名（已执行）

影响面：32 文件、约 110 处，分 5 层——
1. 技能本体 `coding/plan/`（SKILL.md 38 处 + policies/references/prompts/templates 6 个）
2. 跨技能调用：design(5)/build(5)/propose(4)+其 tasks-template(4)/normal(2)+模板(2)/tweak(2)+模板(2)/verify(1)、skills/README.md(5)、commands/polaris-flow.md(1)、design-review-agent(1)/openspec-review-agent(2)/outside-voice(2)/constitution-injection(2)
3. 代码与运行态：`task-state.ts`（`TaskRuntimePlanState`/`runtime.plan`/阶段列表/默认块）、`state-next.ts` PHASE_TO_SKILL `plan:'plan'`、`session-start.ts` REVIEW_AGENTS 的 `plan-review-agent`、`polaris-project-config.ts` phase 枚举、`state.example.yaml`（`plan:` 块 + phase 注释）
4. 伴生资源（待拍板）：`plan-review-agent`→`tasks-review-agent`、`reviews/plan-review-report.md`→`tasks-review-report.md`、`plan_review_status`→?（注意 migrateLegacyShape 读取的**源键** `legacyReview.plan_review_status` 必须保留旧名）
5. 文档与测试：docs/workflow-hooks-call-order.md(4)、test/ts 的 skills-install(2)/install-layout(2)/task-state(1)/hooks-rest(4)

全局替换**禁止误伤**的黑名单：`superpowers:writing-plans`、`executing-plans`、`subagent-driven-development`、`docs/plans/`、`docs/superpowers/plans/`、`testcase_plan.md`（task-kind-layout.ts / task.ts）、`buildInstallPlans`/`PlatformPlan`（init.ts、prompts.ts）、tweak 描述里的「skip brainstorming and full plan」（i18n、command-registration、assets/en）、CHANGELOG 历史条目。

已提示用户的风险：阶段名 `tasks` 与产物 `tasks.md`、运行态目录 `.polaris/tasks/`、hook `tasks-lint`、`tasks-template.md` 全部同名，grep 与口头沟通易混淆（代码层级不冲突）。用户确认接受，按 tasks 执行。

执行结果（2026-09-08 完成，tsc --noEmit 通过，dist 已重建）：
- 目录 `coding/plan/` → `coding/tasks/`（7 文件，git 识别 R）；`agents/review/plan-review-agent.md` → `tasks-review-agent.md`
- 全改名（B 档）：技能名占位符 `--skill tasks`、`--set phase=tasks`、`--phase tasks`、`phase: tasks`、`runtime.plan`→`runtime.tasks`、`TaskRuntimePlanState`→`TaskRuntimeTasksState`、`state-next.ts` `tasks:'tasks'`、`polaris-project-config.ts` phase 枚举、`state.example.yaml` `tasks:` 块与 phase 注释
- 伴生资源全改：`plan-review-agent`→`tasks-review-agent`、`reviews/plan-review-report.md`→`tasks-review-report.md`；**`plan_review_status` 字段名保留**（迁移源键 `legacyReview.plan_review_status` 亦保留旧名）
- 中文「任务规划」保留，仅换 token；启动播报用 `polaris{{SKN_SPR}}coding{{SKN_SPR}}tasks`
- 旧状态兼容：沿用 clarify 先例，`migrateLegacyShape` 新增 legacy 分支「旧顶层 `plan:` → `runtime.tasks`」（放在 stage 数组循环之前），数组内 `'plan'` 改为 `'tasks'`
- 测试：`task-state.test.ts` 两处 `runtime.plan.plan_review_status` 断言 → `runtime.tasks.*`（改名引入的唯一失败，已修）

## coding/propose → coding/plan 全链路改名（已执行）

紧接上一步：`tasks` 占用后 `plan` 名字空出，把原「提案」阶段 `propose` 改名为 `plan`。影响面 38+37 文件、235 行变更。

- 目录 `coding/propose/` → `coding/plan/`；`agents/review/propose-reviewer.md` → `plan-reviewer.md`（**顺带修了既有不一致**：`session-start.ts` REVIEW_AGENTS 原列 `propose-review-agent`，但文件/frontmatter 名一直是 `propose-reviewer`，导致 SessionStart 恒定告警"未安装"）
- 全改名：`--skill plan`、`phase=plan`、`--phase plan`、`phase: plan`、`runtime.propose`→`runtime.plan`、`TaskRuntimeProposeState`→`TaskRuntimePlanState`、`state-next.ts` `plan:'plan'`、phase 枚举、`state.example.yaml` `plan:` 块与 `plan-提案` 注释
- 伴生资源：`propose-reviewer`→`plan-reviewer`、`reviews/propose-review-report.md`→`plan-review-report.md`；**`opsx_propose_status` 字段名保留**
- 中文「提案」保留（phase 注释写 `plan-提案`），只换 token
- **配置键 `model.propose` / `PolarisModelSlots.propose` 未改**（用户可见 config schema，语义是"提案类模型"角色，与 code/review/challenger 并列），改名会让现有 config.yaml 失效

黑名单（本次新增，务必延续）：`openspec-propose` / `opsx-propose` / `/opsx:propose`（OpenSpec 生态命令）、`src/core/integrations/openspec.ts` 的 `ALL_OPENSPEC_WORKFLOWS` 白名单、`test/ts/openspec.test.ts`、`proposal.md`（四件套）、`.cursor/` `.claude/` 第三方资产、`/pofol:propose`（hard-stops.md 里的疑似笔误命令，未动）。

双重改名的历史兼容（关键）：顶层 `plan:` 现在有歧义——新结构是新 plan 阶段（原 propose，含 `opsx_propose_status`），旧结构是旧细计划阶段（含 `tdd_policy`，应归 `runtime.tasks`）。处理：`migrateLegacyShape` 用特征字段分流（含 `tdd_policy` → `runtime.tasks`，否则 → `runtime.plan`），并把 `'plan'` 从 stage 数组移除；另加 legacy 分支「旧顶层 `propose:` → `runtime.plan`」。

上一轮改名遗留、本次一并修掉的漏网（说明链路里的 `**plan**` / 加粗 / `→ plan →` 形式第一轮脚本普遍覆盖不到）：README.md L37 `specify → plan → design(可选) → plan → build`（第二个应为 tasks）、tasks/SKILL.md 链路 `→ **plan** → build`、docs/workflow-hooks-call-order.md 表格第二个 `**plan**` 行、outside-voice.md 阶段表第三行、`openspec-review-agent.md` 的 `Stage: plan|design|plan`、`tasks-review-agent.md` 的 `StandardsRoot: <PLUGIN_ROOT>/plan`、tasks/templates 与 plan/templates 的「**plan** 阶段（…tasks）」。

有意保留的 plan 残留（勿再动）：`plan_review_status`（字段名与迁移源键）、testcase 域 `plan_path` / `testcase_plan.md`、`superpowers:writing-plans` / `executing-plans` / `subagent-driven-development`、`docs/plans/`、`docs/superpowers/plans/`、`buildInstallPlans` / `PlatformPlan`、tweak 描述「skip brainstorming and full plan」、CHANGELOG 历史条目。

测试基线提示（非本次引入，既有失败）：`command-adapters.test.ts`（缺 `src/core/install/command-adapters/command-adapters.js`）、`openspec.test.ts`（引用 `core/integration/` 实为 `integrations/`）、`superpowers.test.ts`（引用 `core/deps/` 实为 `integrations/`）、cli/session-start 系列（依赖全局安装 CLI）、`file-system.test.ts readJson`、`workflow.test.ts` fixture；**另：`session-start.test.ts > 缺 .polaris 目录` 是 5s timeout 性能失败，不是断言失败，跑单文件可复现但不影响判定**。

验证状态（2026-09-08 完成）：`npx tsc --noEmit` 通过；`npm run build` dist 已重建（`plan-reviewer` 已在 dist/core/hooks/session-start.js）；与改名相关的 7 个测试文件全过（task-state 5/5、state-next、skills-install、install-layout、workflow-entry、agents-install、manifest），安装器校验「当前 zh 技能资产无跨技能 ../ 引用、无幽灵占位符」通过。

## commands 全量对齐 + 建 hotfix skill（已执行）

触发：用户原话说"修改一下 commands 中的命令指向"，按"全量对齐 + 建 hotfix skill"档执行。先用 AskUserQuestion 拍板两件事——hotfix 放哪、hotfix 实现深度。两问都选了推荐项（coding/hotfix + 轻量壳）。

改动范围（4 zh 命令 + 4 en 命令 + 1 新建 skill + 3 适配层）：
- **zh/commands/** 4 份：command_prefix 与 trigger 全部从 `pofol` 改为 `polaris`（flow/normal/tweak/hotfix）；hotfix.md 同时把 skill 引用从 `polaris:hotfix` 改为 `polaris{{SKN_SPR}}coding{{SKN_SPR}}hotfix`；hotfix.md 文案 `Use the ...` 改为中文 `使用 ...`（与同目录 normal/tweak 一致）
- **en/commands/**：`polaris.md` 改名为 `flow.md`（与 zh 端 polaris-flow.md 对称），trigger `/polaris:flow`、name 改 `flow`、description 改对齐；tweak.md 与 hotfix.md 改占位符 `polaris{{SKN_SPR}}coding{{SKN_SPR}}<x>`；**新增 `normal.md`** mirror zh 版本（之前 en 端缺 normal——P02 主路径）
- **新建 skill `assets/zh/skills/coding/hotfix/`**（轻量壳，3 文件）：
  - `SKILL.md`：4 步 brief→tasks→fix→ship，跳过 constitution/split-precheck/exit-check 三道守门；保留 ship 守门（hotfix 的"紧急"是相对 tweak 的"轻量澄清"而言，不接受绕过 ship）；hard-gate 显式列出哪些"禁止"——根因未定位/范围跨 3+ 模块/schema 变更/对外 API breaking change 全部强制回 normal
  - `policies/incident-recap-template.md`：事故复盘骨架（5 Whys ≥3 层 + 后续行动 ≥1 条 + 14 天内回写）
  - `templates/hotfix-tasks-template.md`：≤3 task、必含 `<!-- TDD 任务 -->`（bug 修复 = 必写回归测试）、incident 复盘任务可选
- **适配层**：`assets/zh/adapters/command-registration.md` 表更新（4 命令：/polaris:flow/:/normal/:/tweak/:/hotfix；旧的「/polaris 启动完整工作流」一行删掉）；`assets/shared/templates/constitution-template.md` 把错误的 `/pofol:constitution amend` 改为 `polaris{{SKN_SPR}}constitution` 技能路径；`assets/zh/policies/hard-stops.md` 2 处 pofol 残留（"所有 `/pofol:*` 命令"与"plan skill 的 Step 1 在 `/pofol:propose` 入口"）

**关键发现（避免下次踩坑）**：
- **`maintance` 不是合法 family**——`src/core/install/skills.ts` 的 `SKILL_FAMILIES = ['coding', 'prd', 'testing']`，且 `shouldSkipSkillShortPath` 也没跳它。若按"原计划"建 `assets/zh/skills/maintance/hotfix/SKILL.md`，会被 `parseSkillAssetPath` 判为顶层叶技能（family=null），命名变 `polaris:hotfix` 而不是预期的 `polaris:maintance:hotfix`，且和 `constitution`/`subagent-probe`/`subagent-dispatch` 一样成为顶层叶技能。`assets/zh/skills/maintance/` 这个空目录从 09-03 起就是占位，从来没实现过。**解法：hotfix 放 `coding/hotfix/`，不引入新 family**。
- **trigger 解析不依赖 trigger 字符串**——`installPolarisCommandsForPlatform` 把整个 .md 文件当命令模板复制过去，trigger 只是 frontmatter 元数据；trigger 改 `pofol:flow → polaris:flow` 不影响安装逻辑，只影响宿主命令面板的显示。

**有意保留的不一致（记入"待办"）**：
- `assets/zh/skills/README.md` L21 仍写「命令入口一般为 `/polaris-flow-<阶段>`」——老描述，实际命令是 `/polaris:flow` + `use_skill` 加载阶段技能。本次未改，等用户决定。

### 追加：command 前缀 polaris → pofol（2026-09-08 19:55+ 后续）

用户要求把 10 份 command 文件（zh 5 + en 5）的前缀（`command_prefix` + `triggers`）从 `polaris:` 改回 `pofol:`。skill 命名空间 `polaris{{SKN_SPR}}coding{{SKN_SPR}}<x>` 不动（用户拍板"只改 command 文件"）。

**改动**：每个文件仅 frontmatter 的两行（位置：第 3-4 行）——共 20 行替换。
- `command_prefix: polaris` → `command_prefix: pofol`
- `triggers: ["/polaris:<x>"]` → `triggers: ["/pofol:<x>"]`

**未改**：
- `command-registration.md` L59 的教学文字（"frontmatter 含 `name` / `command_prefix` / `triggers` / `description`"），只是字段说明、不指定具体值
- 任何 skill 引用形式的 `polaris{{SKN_SPR}}coding{{SKN_SPR}}<x>`
- `hard-stops.md`、`complexity-router.md` 等文档（已查 grep 无 `/polaris:` 字符串引用）

**未提交**：工作区现有 6 批未提交改动混在一起（4 次阶段改名 + commands 全量对齐与 hotfix skill 新建 + 本次 polaris→pofol），建议 6 次独立 commit，每次单独写 message。

**操作 SOP**：只改 frontmatter 的两个特定字段 → 用 Python `str.replace(x, y, 1)` 限制替换次数为 1（防止替换到文件其他位置）→ 用 Python 读 frontmatter 复核全 10 份。

**有意保留的 conicidence**：
- en/commands/flow.md 的描述比 zh 薄——zh 端 polaris-flow.md 是完整的 11 项功能菜单，en 端只是入口 stub。两者语义对应（都是 flow 入口），但厚度不对称。**未做翻译**，等用户决定要不要同步。

验证：tsc 通过、npm run build 通过、3 个测试文件（21 用例）全过、安装器跨技能引用校验通过。

## polaris-flow 前置预检（零步）+ 自动评估路由（已执行）

触发：用户原话"在执行前先检查是否存在需求文档或描述需求内容...根据复杂度进行路由。简单需求走tweak，常规需求走normal，复杂需求走sdd"。

先 4 个 AskUserQuestion 拍板：
1. **sdd 是什么** → 复现 P03 现有链路（specify 起头的 8 阶段），后来发现 `assets/zh/commands/sdd.md` 预存在（未跟踪文件，描述写"SDD 驱动开发"但内容错引用 polaris:twk），二次拍板"复用 P03（sdd 作为别名）"
2. **强制附需求范围** → 只对开发类强制（推荐）
3. **自动评估机制** → 复用 idea-discovery 4 档（simple/standard/complex/needs_split）
4. **持久化** → 单次会话（不持久化）

改动范围（2 命令 + 1 共享策略 + 1 新文件）：
- **`assets/zh/commands/polaris-flow.md`**（22.3KB → 大幅扩展）：
  - HARD-STOP 加第 11 条"开发类无需求禁止加载技能"
  - 「执行状态」加 `需求内容` 字段（4 个状态：`已选类别` / `已选功能` / `需求内容` / `附加上下文`）
  - **新增「零步：前置需求预检（仅开发类强制）」**（0.1 识别 / 0.2 强制收集 / 0.3 询问自动评估 / 0.4 跑 4 档评估 / 0.5 状态衔接）
  - 「第四步」表里 M01 修旧 bug（原来写 `maintance/hotfix` 标"暂不可用"——错）+ 注脚（"需求内容"作为独立字段交接给被加载技能，约定 tweak/normal/specify/hotfix 各自的读取方式）
  - 「复杂度判定」加注脚（"或走零步 0.4 自动评估覆盖 P01/P02/P03"）
- **`assets/zh/commands/sdd.md`**（预存在 197B 残文件 → 完整重写）：trigger 改 `polaris:sdd`、技能引用改 `polaris{{SKN_SPR}}coding{{SKN_SPR}}specify`（旧文件错指 tweak）、加别名说明（与 P03 等价 + 触达路径差异 + zero-step 预检联动）
- **`assets/zh/commands/policies/complexity-router.md`**（新文件，封装 idea-discovery 4 档）：
  - 4 档判定信号：模糊点 / 范围 / 方案分叉 / 风险
  - 评分规则：4 信号 0-2 分求和（0→simple / 1-3→standard / 4+→complex）
  - 强制升档：安全/数据迁移/对外 API breaking 强制 complex；任何信号到 complex 强制 complex；用户提"分多期/拆里程碑"触发 needs_split
  - 路由映射：simple→tweak / standard→normal / complex→specify（P03）/ needs_split→STOP
  - 与 33 分制策略（`prd/discovery/policies/complexity-assessment-policy.md`）的关系：flow 入口用 4 档轻量判定，PRD 阶段用 33 分制细粒度评分，两者均遵循"宁严勿松"
- **`assets/zh/policies/hard-stops.md`**：新增 H14「开发类路径禁止无需求内容时加载技能」+ 自检话术（适用 skill=`polaris:flow`（零步），违规后果=立即停止）
- **`assets/zh/adapters/command-registration.md`**：L44 `/polaris:flow` 描述改写为"开发类路径走前置需求预检 → 强制附需求 → 询问自动评估复杂度 → 按 4 档路由；其他类直接菜单选择"
- **补改**：`polaris-flow.md` / `tweak.md` / `hotfix.md` / `normal.md` 4 份命令文件的 trigger `pofol:` → `polaris:`（上次"全量对齐 commands"漏改）

**新增 en 镜像**：`assets/en/commands/sdd.md`（en 端原本没有 sdd，按对称补上）。

**关键决策回顾**：
- **sdd = P03 别名**（不新建独立 skill）——避免与现有 specify 起头的 8 阶段链路重复；`/polaris:sdd` 适合"已知复杂直接进 8 阶段"的用户，`/polaris:flow` 适合"未决定复杂度、让命令预检"的用户
- **needs_split 不是路由到任意 skill，而是 STOP**——避免"4 档路由变 5 档"的歧义
- **零步强制但不是"无任何上下文就阻断"**——0.1 识别 4 个判定规则（动作+目标+2 项、关键字 80 字以上、附加文件含需求要素、PRD/OpenSpec 制品目录），命中任一即视为有需求
- **预检硬上限 2 轮**——超过 2 轮用户仍坚持空需求则强制 complex（让复杂度评估兜底）

**踩坑**：
- 我在 AskUserQuestion 选项里把 `{{SKN_SPR}}` 误写成了 `{{SKN_SPLITTER}}`（中英文相似的拼写错误），写入到 polaris-flow.md 与 sdd.md 后被 grep 自检发现，立刻全部修回 `{{SKN_SPR}}`
- sdd.md 是预存在的未跟踪文件（创建时间 19:32），不在我前面三次改名的工作流里——这次发现后走"复用 P03（sdd 作为别名）"决策

**有意保留的不一致**：
- en/commands/flow.md 的描述比 zh 薄（与上次同）
- en 端 hotfix / tweak / normal 的"使用 `polaris{{SKN_SPR}}coding{{SKN_SPR}}<x>` 技能"格式没问题，但 trigger 字符串与 zh 一致是 `/polaris:<x>`——en 端 trigger 也用 polaris 前缀（与 zh 一致）

验证：tsc 通过、build 通过、3 文件 21 用例测试全过、安装器跨技能引用校验通过、`pofol` 残留归零、`{{SKN_SPLITTER}}` 笔误残留归零。
