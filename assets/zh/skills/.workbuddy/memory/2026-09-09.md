# 2026-09-09

## README-zh.md「阶段工作流」段重写（约 09:55）

原 L40-61 段严重过时：trigger 错写（`/polfo:xxx`、`/ptfl:xxx`、`/ezfl:xxx`）、链路仍用旧名（propose/plan 混用、`plan→tasks` 前的版本）、没有体现最新的 P01/P02/P03 三档与 `polaris:flow` 复杂度路由。

**新结构**（L40-110 共 71 行，替换原 22 行）：
1. 顶部 2 行导读：阶段名中英双轨原则 + 沿革告示（指向 2026-09 三次改名）
2. §1 SDD 工作流（P03 完整链路）：含 design 可选 + retro 可选
3. §2 常规需求工作流（P02）：评测压缩说明（合并主审、不含 OV、tasks 一次终版）
4. §3 小改动工作流（P01）：`change-brief.md` + `tasks.md` 产物
5. §4 紧急 Bug 修复（P01 变体）：hotfix 省略项 / 硬约束 / 强 TDD
6. §5 入口路由策略：前置需求预检 + 复杂度评估 4 档路由表 + 直达命令豁免

**保留不动**（用户明确「更新内容 阶段工作流」）：L7-13 核心能力表（assets 路径仍写 `assets/skills/` 是过时的）、L18 pnpm 版本（实际用 npm/managed node）、L33-38 计划命令（仍是 `polaris init/status/...`）。

**未提交**：与昨日 6 批改动混在一起，建议 7 次独立 commit。本次 message 候选：`docs(readme-zh): rewrite 阶段工作流 section per post-rename state`。

## 复杂度路由 4 档 → 3 档，needs_split 并入 complex（约 10:22）

用户提问：复杂度判定不需要区分「是否拆分」，需要拆分的需求识别为 standard 是否合适？

**结论（查证后）**：归 standard 不合适；needs_split 应从入口删除、并入 complex。

**事实链**：
- `task-split-precheck` 只存在于 `coding/specify/` 和 `coding/tweak/`，`normal` 没有——normal 的 tier-gate 只有「降档门」（太小降 tweak），无「拆分」
- specify 的 `task-split-precheck` 是完整的「需求目标拆分」机制（规模检测 → 候选拆分清单 → 决策点 A/B/C → 批量模式）
- 所以「需要拆分」唯一正确归宿 = complex → specify，由 precheck 接手；入口 needs_split → STOP 是重复闸门（拆两次）

**改动 3 文件**：
1. `complexity-router.md`：标题/头部/评分表头/强制升档/输出格式/路由映射/边界处理/33分制关系共 11 处，needs_split 全部删除或改写为「并入 complex」
2. `polaris-flow.md`：0.3 描述、0.4 评估说明、路由覆盖表（**顺带修了重复表头 bug** L154-157）、needs_split 说明段，共 4 处
3. `README-zh.md`：复杂度评估 4 档→3 档 + 删 needs_split 行 + complex 承接说明

**残留核验**：needs_split 归零（唯一保留的是 complexity-router 头部「原 4 档」历史对照说明，属有意保留）。

**坑（重要）**：BSD grep 对中文 pattern 会静默漏匹配——`grep -rn "needs_split"` 在 polaris-flow.md 上返回空但文件里明明有（L161）。核验残留必须用 Python 读文件逐行检查，不能信 grep。

**长期约定已记入 MEMORY.md**：「复用价值高的既有资产」加一条——polaris-flow 入口复杂度路由 3 档，needs_split 并入 complex（2026-09-09 拍板）。
