/**
 * A3：把 SKILL.md §六「视觉与布局红线（浓缩）」收回成真正的摘要层。
 * 原则：保留行为红线（不能做什么），把与 references 重复的具体数字删掉、改为指向出处；
 *      并顺手修掉一条已经过期的指令（大文件编辑纪律 → 已由 scaffold 从根上取代）。
 * 断言式替换：切片边界必须唯一命中，否则不写盘。
 */
import fs from 'node:fs';

const FILE = process.argv[2];
const src = fs.readFileSync(FILE, 'utf8');
const lines = src.split('\n');

const start = lines.findIndex((l) => l.startsWith('## 六、视觉与布局红线'));
const end = lines.findIndex((l) => l.startsWith('## 七、研发交付与需求追踪'));
if (start < 0 || end < 0 || end <= start) throw new Error(`切片边界定位失败：start=${start} end=${end}`);
if (lines.filter((l) => l.startsWith('## 六、')).length !== 1) throw new Error('§六 标题不唯一');
if (lines.filter((l) => l.startsWith('## 七、')).length !== 1) throw new Error('§七 标题不唯一');

const block = [
  '## 六、视觉与布局红线（浓缩）',
  '',
  '> **本节是摘要，不是第二份规范。** 完整判据与阈值一律以 `references/` 为准；本节的职责只是让红线不被漏掉。',
  '> 括号里的 § 号就是该条的唯一出处——数字型阈值不在本节重复。',
  '',
  '**字号**：大面积正文不得为了塞内容压到 11～12px，1920×1080 下不得呈现「小字缩放版后台」；表格正文、Sidebar 菜单、按钮文字用正文级字号。底线口径与脚本分工见 `§15.2`。',
  '',
  '**密度**：标准 / 紧凑 / 展示三种密度模式，同系统页面密度逻辑必须一致；紧凑密度靠减少 Padding，不靠缩小字体。定义见 `§15.5`。',
  '',
  '**布局高度**：三种内容感知模式——Auto Content（内容决定高度）、Adaptive Data（数据量自适应，分页紧跟数据）、Full Workspace（任务决定高度）。容器空置率、内部滚动门槛、看板卡片与首屏占用的判据见 `§16.3`。',
  '',
  '**命名与品牌**：Header 原则上只允许 1 个视觉主系统名称，系统名称在登录页、浏览器标题、Header、导出文件等位置保持一致。细则见 `§16.1`（**阶段 3 就要用**）。',
  '',
  '**响应式**：必检 1366×768 / 1440×900 / 1920×1080 三档（建议加 2560×1440）；禁止「截图式布局」；宽屏增加的是工作空间，不是把元素等比拉长。规则见 `references/06`。',
  '',
  '**对比度与兼容**：文本 Token 对白底有对比度硬指标（**`verify L1-9` 强制，见 `§14.3`**），禁止凭「看起来还行」定灰度；2023+ 新 CSS 特性禁止在关键视觉路径裸用，必须配显式兜底或不采用；问候语、日期等动态内容禁止硬编码在 HTML 里（`verify L1-16` 会提示）。',
  '',
  '**图标与高级感**：高级感来源优先级 **图标体系 > 板块细节 > 交互形式 > 色彩微调**；收到「不够高级 / 不够科技感」反馈时，禁止第一反应换成深色主题或更换主色。界面图标必须全站一套内联 SVG symbol 线性体系，禁止混用彩色 emoji、字符图标与图片图标（`verify L1-10` 强制）。做法见 `§24.3`。',
  '',
  '**演示数据自洽**：同一业务单号 / 对象在所有页面状态一致（单一数据源）；费用类数字口径统一、不得出现无来源数字；跨页面引用的日期金额与来源页一致；Tab 计数用真实可数数据。矛盾按 P0 缺陷处理，判据见 `references/07`。',
  '',
  '**交付门禁（强制）**：交付前必须运行 `scripts/verify.mjs` 全三层并取得**退出码 0**（静态 → 逐页冒烟 → 黄金流链路 + 三档分辨率）；退出码非 0 不得交付，WARN 项需人工判断。**脚本只判「有没有」，判不了「好不好」**——设计质量仍按 `references/07` 人工审查。规格见 `§27.1`。',
  '',
  '**文件编辑（强制）**：骨架与增删一律走 `scripts/scaffold.mjs`（`init` / `add-page` / `add-icon` / `set-tokens`）——它用区域标记做定点插入，锚点命中不唯一即中止且不写盘。**禁止对同一份原型并行发起多条编辑**（并发写会互相覆盖）。详见 `§27.2`。',
  '',
  '**演示数据去个人化（强制）**：禁止出现用户本人或真实同事的真实姓名、手机号、工号、真实内部项目名；统一用通用演示身份（如「林远 / 陈默」等明确虚构名），姓名集中在数据区定义供一键替换。**已由 `verify L1-15` 自动扫描**，见 `§27.3`。',
  '',
  '---',
  '',
];

const next = [...lines.slice(0, start), ...block, ...lines.slice(end)];
fs.writeFileSync(FILE, next.join('\n'));
console.log(`§六 重写完成：${end - start} 行 → ${block.length} 行；文件 ${lines.length} → ${next.length} 行`);
