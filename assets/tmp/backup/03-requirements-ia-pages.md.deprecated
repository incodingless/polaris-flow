# 6. 标准流程（9 个阶段）

阶段 1–5 在本文件；阶段 6–7 见 `references/04`，阶段 8 见 `references/05 §14`，阶段 9 见 `SKILL.md §四`。

## 阶段 1｜需求理解与产品快速定义

先形成：

# 《Web 原型任务理解卡》

至少包含：

- 产品一句话定位；
- 主要用户；
- 使用场景；
- 当前问题；
- 核心目标；
- 3～5 个核心任务；
- 核心业务成果；
- 一期范围；
- 已知信息；
- 待确认信息。

### 需求转换公式

> **原始需求 → 用户 × 场景 × 任务 × 问题 × 结果**

例如：

“做 AI 合同审查系统”

应转换成：

> 合约专员  
> + 审查客户合同  
> + 快速发现风险并形成正式审查成果  
> + 当前人工审查时间长  
> + AI 先审、人确认。

### Gate 1
需求理解确认后进入任务流设计。


# 7. 阶段 2｜黄金任务流 Golden Flow

本技能 必须找到：

> **最能体现产品价值的一条完整任务闭环**

例如：

> 上传合同 → AI 解析 → AI 审查 → 查看风险 → 查看依据 → 人工确认 → 生成审查成果

或者：

> AI发现隐患 → 查看详情 → 派单 → 整改 → 复核 → 闭环。

### 黄金任务流原则

1. 先保证闭环；
2. 优先服务高频、高价值任务；
3. 页面围绕任务生成；
4. 不为“功能丰富”破坏任务闭环；
5. 演示/竞赛模式下原则上只保留 1～2 条黄金任务流。

### 输出

# 《核心用户任务流》


# 8. 阶段 3｜信息架构 IA

先设计产品结构，不直接画页面。

## 8.1 信息架构原则

- 高频业务优先；
- 一级导航控制数量；
- “管理”和“配置”能力后置；
- 不为“大系统感”堆菜单；
- 一个导航项必须能对应明确用户任务；
- 同一业务对象尽量统一入口；
- 管理、业务处理、分析、资产配置区分清楚。

## 8.2 推荐基础结构

```text
工作台
├─ 我的待办
├─ 重点提醒
└─ 最近任务

核心业务
├─ 新建 / 发起
├─ 处理中
├─ 已完成
└─ 业务详情

分析 / AI
├─ AI分析
├─ 风险 / 异常
└─ 分析结果

资产
├─ 模板
├─ 规则
└─ 知识

系统
├─ 用户
├─ 权限
└─ 参数
```

该结构只是候选，不得机械套用。

### 输出

# 《Web 产品信息架构 IA》


# 9. 阶段 4｜页面体系与职责划分

本技能 **不对页面数量设置机械上限或下限**。

页面数量必须由以下因素自然推导：

> **用户任务 + 业务闭环 + 信息架构 + 页面职责 + 交互复杂度**

不得为了“页面少”而强行合并本应独立的复杂任务，也不得为了“系统看起来完整”而创建没有明确用户任务的页面。


## 9.1 页面存在的必要性判断

一个页面至少应满足以下一项：

1. 承载一个明确、独立的用户任务；
2. 承载一个需要持续操作的复杂业务对象；
3. 是核心业务闭环中的必要阶段；
4. 需要独立的信息空间才能保证认知清晰；
5. 内容复杂度已不适合通过 Dialog / Drawer 承载；
6. 承担独立的分析、决策、审核、配置或工作任务。

如果一个页面无法回答“用户为什么需要进入这个页面”，应重新审查其存在必要性。


## 9.2 页面职责原则

> **不追求页面多，也不追求页面少；追求页面职责清晰、用户任务完整。**

一个页面应有明确的主任务。

当一个页面同时承担过多复杂任务，造成以下问题时，应考虑拆分：

- 信息过载；
- 操作路径混乱；
- 用户需要频繁在不同上下文之间切换；
- 页面状态过多且难以理解；
- 主操作不清晰；
- 研发实现和测试边界不清。

反之，如果两个页面只是形式不同、任务高度一致，也应考虑合并或使用 Tab / Drawer / 分区方式承载。


## 9.3 页面清单输出

本技能 应形成：

# 《Web 页面清单与页面职责说明》

至少包含：

- Page ID；
- 页面名称；
- 使用角色；
- 页面主任务；
- 主要业务对象；
- 主要入口；
- 主要出口 / 下一步；
- 对应业务场景 / 用户任务；
- 是否存在待产品确认事项。

是否进一步划分优先级、建设批次、实现范围或实现深度，由产品经理根据项目实际决定——本技能 不默认替产品经理做该项决策（完整边界见 `references/00-basis.md §二（§31.2–§31.3）`）。


# 10. 阶段 5｜页面模式选择

本技能 不应所有页面都使用“查询 + 表格 + 弹窗”。

根据任务自动选择合适模式。


## 10.1 标准工作台

适用：

> 高频入口、待办、提醒、关键任务、重点指标。

布局重点：

- 今日待办；
- 高优任务；
- 异常提醒；
- 最近处理；
- 快捷发起。

不要把工作台做成纯大屏。


## 10.2 标准列表页

适用：

> 查询、筛选、批量管理、业务对象管理。

标准结构：

> 页面标题  
> → 查询区域  
> → 工具栏  
> → 数据表格  
> → 分页

- 查询表单；
- 操作区；
- 表格；
- 分页；
- 操作列。


## 10.3 对象详情页

适用：

> 项目、合同、任务、隐患、档案、人员等业务对象。

建议结构：

> 标题 / 状态 / 关键操作  
> → 关键基础信息  
> → Tabs / 分区  
> → 业务记录 / 时间线 / 附件 / 关联对象。

信息量较大时，优先独立详情页，不强制弹窗。


## 10.4 主从式工作页

适用：

> 左侧任务 / 对象列表 + 右侧详情。

优势：

- 切换快；
- 上下文连续；
- 适合审核、处理、任务中心。


## 10.5 对照审核页

适用：

> 合同审查、档案审核、图纸核验、AI审查。

推荐：

> 左：原文 / 原对象  
> 右：AI结果 / 风险 / 差异  
> 下或侧：依据 / 修改建议 / 人工确认。


## 10.6 流程办理页

适用：

> 审批、审核、整改、复核。

应显示：

- 当前状态；
- 当前节点；
- 历史记录；
- 待办事项；
- 当前操作。


## 10.7 配置管理页

适用：

> 规则、知识、模板、标签、参数。

推荐：

> 左侧目录 / 树  
> +  
> 右侧配置 / 编辑。


## 10.8 分步向导页

适用：

> 上传 → 配置 → AI处理 → 人工确认 → 完成。

优先使用 Stepper 清晰表达当前进度。


## 10.9 AI 工作台

适用：

> Agent、AI分析、AI审查、AI生成。

必须包含：

- 任务输入；
- AI 当前状态；
- AI执行步骤；
- 结构化结果；
- 依据；
- 人工确认；
- 后续业务动作。
