# 2026-09-07

## coding / verify 技能盘点（用户提问：当前做了哪些工作）

- coding 家族共 10 个阶段技能 + 2 个单入口通道（tweak / normal），8541 行；三档链路 P01 tweak→ship / P02 normal→ship / P03 clarify→propose→(design)→plan→build→verify→ship→(retro)。
- verify（355 行）职责：Step0 入口校验 → Step1 dirty worktree → Step2 Constitution 审计（注入点 D）→ Step3 5 个 scorer 加权 overall_score + mode 分发（solo 告警 / team 阻断）→ Step4 light/full 实现验证（启发式：>3 tasks / >1 capability / >8 变更文件 → full）→ Step5 落盘 verify-report.md + metrics + phase=ship。

### 盘点中发现的待修问题

- **路径 bug（新）**：`verify/SKILL.md` 三处引用 `./reference/decision-point.md`（硬闸门、Step 0 多匹配、验证失败决策），但 verify 目录下只有 `policies/`，无 `reference/`；家族约定是 `./policies/decision-point.md`（由 `assets/zh/policies/` 打包注入）。prd 各技能均写 `./policies/`，verify 是唯一写错的。
- Step 5 内编号错乱：`1. 2. 4. 3.`（推进与输出顺序颠倒）。
- verify 只做静态审查（Constitution + scorer + 对照 specs），不执行端到端/接口测试——与重构蓝图第 ③ 项（新增 test 阶段）对应，`assets/zh/testing/` 仍只有 2 个文件，`coding/test/` 未建。
