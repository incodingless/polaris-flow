# 2026-09-17 工作日志

## debug 族「decision-point 引用过密」评审（提出，未改动）

**问题**：debug 族 SKILL.md 里大量「按 `./policies/decision-point.md` 暂停/询问」。全仓实测：
SKILL.md 内 104 行含该引用；写法分布 —— 完整路径字面量 86 次、裸名 `decision-point` 48 次、
`decision-point.md` 32 次、「决策点协议」10 次；另发现 **2 处拼错成 `./policy/decision-point.md`**
（`coding/build/SKILL.md:61,85`，路径不存在）。

**结论（分析，未落改）**：
1. 停顿点标注本身**必要**（是 debug 族"每步带证据 / 三道人确认"的载体），但**全路径重复不必要**。
2. 一行引用装载三层：① 语义（这是阻塞点、不得代选）② 定义位置（读哪个文件）③ 本次内容（决定什么 + 选项）。
   ② 是低信息密度的重复字面量，上下文压缩时最易被合并/丢弃；① 若只隐式存在于被引文件中，也会连带丢失。
   → **标记必须自解释**，路径只声明一次。
3. **误用约半数**：按 `decision-point.md` 自己的定义（≥2 个均合法且改变范围/风险/不可逆结果的选项），
   diagnose 2.1「索要 Jira 单号」、2.3「缺项一次问齐」、5.0「止血状态」都不是决策点，而是信息收集 / 状态申报门禁。
   戴 DP 帽子会 ① 逼该快的地方造选项 ② 稀释真决策点的严肃性。
4. **降级策略冲突**：`decision-point.md`（失败一次本会话永久降级）与 `ask-question-react.md`（每次重试）
   规定相反，靠"调用方按场景选用"；`coding/specify/SKILL.md:126` 一句话内同时引两个文件 → 注入点随机。
5. **提案**：对齐已有 H 系列的「短 ID + 单一权威表」范式（H8/H12 就是现成样板）——
   正文改 `⛔D3：是否建 worktree — A 建 / B 留在主仓`；SKILL.md 约定章节声明一次「⛔D 适用 decision-point 协议」
   + 一张 3–5 行决策点清单表（同时解决压缩恢复）；信息索要换另一类标记或直写「索要：…」；
   必须跨会话恢复的决策点落 state 字段（`runtime.plan.worktree_decision` 已是此模式）。

**状态**：待用户拍板，尚未改任何技能文件。

## 记忆维护

- `MEMORY.md` 超限被截断，已就地精简重写（215 行 → 约 150 行）：保留全部判据与约定，
  删除已闭合项的过程叙述（G1–G6 压成一行、S1 记「已闭合」），新增上面 D1 待决项。
