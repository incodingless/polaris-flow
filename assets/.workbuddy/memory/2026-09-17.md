# 2026-09-17 工作日志

## debug 族「decision-point 引用过密」评审（提出，未改动）

**问题**：debug 族 SKILL.md 里大量「按 `./policies/decision-point.md` 暂停/询问」。全仓实测：
SKILL.md 内 104 行含该引用；写法分布 —— 完整路径字面量 86 次、裸名 `decision-point` 48 次、
`decision-point.md` 32 次、「决策点协议」10 次；另发现 **2 处拼错成 `./policy/decision-point.md`**
（`coding/build/SKILL.md:61,85`，路径不存在）。

**结论（分析，未落改）**：
1. 停顿点标注本身**必要**（是 debug 族"每步带证据 / 三道人确认"的载体），但**全路径重复不必要**。
2. 一行引用装载三层：① 语义（这是阻塞点、不得代选）② 定义位置（读哪个文件）③ 本次内容（决定什么 + 选项）。
   ② 是低信息密度的重复字面量，上下文压缩时最易被合并/丢弃；① 若只隐式存在于被引文件中，也会连带丢失。
   → **标记必须自解释**，路径只声明一次。
3. **误用约半数**：按 `decision-point.md` 自己的定义（≥2 个均合法且改变范围/风险/不可逆结果的选项），
   diagnose 2.1「索要 Jira 单号」、2.3「缺项一次问齐」、5.0「止血状态」都不是决策点，而是信息收集 / 状态申报门禁。
   戴 DP 帽子会 ① 逼该快的地方造选项 ② 稀释真决策点的严肃性。
4. **降级策略冲突**：`decision-point.md`（失败一次本会话永久降级）与 `ask-question-react.md`（每次重试）
   规定相反，靠"调用方按场景选用"；`coding/specify/SKILL.md:126` 一句话内同时引两个文件 → 注入点随机。
5. **提案**：对齐已有 H 系列的「短 ID + 单一权威表」范式（H8/H12 就是现成样板）——
   正文改 `⛔D3：是否建 worktree — A 建 / B 留在主仓`；SKILL.md 约定章节声明一次「⛔D 适用 decision-point 协议」
   + 一张 3–5 行决策点清单表（同时解决压缩恢复）；信息索要换另一类标记或直写「索要：…」；
   必须跨会话恢复的决策点落 state 字段（`runtime.plan.worktree_decision` 已是此模式）。

**状态**：待用户拍板，尚未改任何技能文件。

## 记忆维护

- `MEMORY.md` 超限被截断，已就地精简重写（215 行 → 约 150 行）：保留全部判据与约定，
  删除已闭合项的过程叙述（G1–G6 压成一行、S1 记「已闭合」），新增上面 D1 待决项。

---

## 第一版改造被否决并全部回退（同日）

**做了什么**：在 `decision-point.md` 加「停顿标记」节定义三个标记；在每个 debug SKILL.md 加
「停顿标记」章节 + **决策点清单索引表（D1–D7）**；正文 22 处长路径换 `【决策点 Dn】` / `【索要】` / `【停止】`。

**用户否决**：「这样子修改不行。后面再做技能优化，难度就太高了，阅读技能也不方便。」

**错在哪**（已写进 `MEMORY.md §一`）：
1. **ID 是编码不是语义**——`【决策点 D6】` 必须跳表才知道是什么，比原来的长路径**更难读**。
2. **表 ↔ 正文构成新的双源**——改 Step 要同步重编号与表，正是仓库 `单源判据` 禁止的。
3. 加了 3 个新概念、3 个新章节、22 处替换，改动面远大于收益。

**正确方向（用户选定，已落地）**：站点只写自解释的中文 `**暂停等用户选**`，**不引入编号、不建表、不新增章节**；
协议指针只在 SKILL.md 里**已有的 policies 索引行**末尾追加一句（零新增结构）。

**最终改动（3 文件 / +23 −31 行）**：
- `diagnose`：HARD-GATE 索引行补 `；停顿（暂停等用户选 / 信息索要 / 阻塞报告）见 ./policies/decision-point.md`；
  Step 1 / 2.1 / 2.2 / 2.3 / 3.1.A / 5.0 / 5.6 / 6.5 八处站点改白话。
  Step 3.1.B（hotfix 分支被脏工作区挡住）**删掉那组假 A/B**——两个选项都导向同一动作，改写成
  「报告阻塞原因与恢复条件」。
- `patch`：索引行同上；Step 0 两处 + Step 1.5 改白话。
- `closeout`：索引行同上；Step 0 两处 + Step 4 提交方式 + Step 5 发布确认改白话。
- `bugfix` / `hotfix` / `README` / 共享 `policy` **未动**（用户回退后保持原样；README 不入仓也不随技能安装）。

**验证**：`npx vitest run test/ts/skills-install.test.ts test/ts/commands-install.test.ts` →
skills-install 13/13 通过；commands-install 仍是那 1 个 prove 缺失的既有失败（改前改后同一断言同一报错）。
`git status` 只剩这 3 个技能文件 + memory。

## 顺带查出的既有缺陷（未修）

1. **`assets/zh/skills/debug/prove/` 目录不存在**，但 `bugfix`/`hotfix` 装配表、`hotfix` 加严项、
   `README` 技能一览与目录树仍按六段（含 `prove`）写；`triage`/`prescribe` 已被同日三阶段合并进 `diagnose`。
   → `test/ts/commands-install.test.ts` 的 debug 族用例恒红（ENOENT .../debug/prove/SKILL.md）。
   **已确认非本次改动引入**（改动前 `ls` 即无该目录，`git status` 无删除记录）。
   `test/ts/skills-install.test.ts` 13/13 通过。
2. `coding/build/SKILL.md:61,85` 把策略路径抄成 `./policy/decision-point.md`（少 `ies`，路径不存在）。

## 工具教训

- 本机 BSD grep 不吃双引号内的 `\|` 交替，**中文多字节模式下静默返回空**——本次误判为「替换失败」，
  实际是 grep 假阴性。核验中文内容一律用 Grep 工具（ripgrep）。
- 仓库根 `.git/index.lock` 会在并发的 git 进程/编辑器操作中出现；**先看文件大小与 mtime 判断是否活跃**
  （81KB + 刚写入 = 有真进程，绝不可删；0 字节 + 陈旧 = 可清）。


---

## debug 族迁移盘点（19:05，已查清，待用户定入口层）

**现状**：阶段已合并为 3 个 —— `diagnose`（原 triage+diagnose+prescribe）、`patch`（并入原 prove 的独立验证，见 patch Step 1.5）、
`closeout`。入口仍两个：`bugfix` / `hotfix`。

**结论：两条通道的装配已完全相同**（diagnose → patch → closeout）。证据：
1. `src/core/hooks/state-next.ts:93` `DEBUG_PHASE_TO_SKILL` 的 bugfix / hotfix 两张映射**逐项相同**
   （去掉 prove 后），channel 对「下一段是谁」已**零影响**；
2. 差异只剩四处，且全部由 `channel` 字段在各**阶段技能内部**表达：加严项（diagnose 保全/止血、
   patch 数据脚本/埋点/开关 + 1.5 独立验证、closeout 发布确认）、人在环节点 3 处、
   工作区（worktree vs hotfix 分支）、人确认点数（1–2 vs 3）；
3. 通道差异被写在**三处**（`_shared/scene-routing.md` 边界表 / `bugfix/SKILL.md` / `hotfix/SKILL.md`）
   → 漂移已实际发生：bugfix 已改三段，hotfix 仍写六段。

**陈旧引用清单（12 个文件，全部仍写 triage / prescribe / prove）**：
| 文件 | 陈旧点 |
|---|---|
| `hotfix/SKILL.md` | 全文：六段装配、加严 6 项、人在环表、各阶段技能、工作区、运行态 |
| `bugfix/SKILL.md` | description「五段」；L25 `prescribe` 出口；L31-33 各阶段技能表 5 行；L39 `triage` 段 |
| `patch/SKILL.md` | L9 `prescribe` 未过门禁；L25 先走 `debug:prescribe`；L81/91 `prescribe` 改动点清单；L136 `next-phase <closeout\|prove>`；L138 回 `prescribe`；description 仍指 `prove` |
| `closeout/SKILL.md` | description 指 `debug:prove`；L9「`patch`/`prove` 未过」 |
| `diagnose/SKILL.md` | description 不触发项仍写「独立验证（走 prove）」 |
| `patch/templates/verification-template.md` | L4、L20「由 `prove` 回填」 |
| `_shared/artifacts.md` | L9-12、22-27（triage/prescribe/prove 归属） |
| `_shared/scene-routing.md` | L28 阶段装配行；L38「`triage` 之前」 |
| `_shared/capability-tiers.md` | L19 `triage` 段；L23 `prove` 段 |
| `README.md` | mermaid、技能一览、阶段详解、目录树、使用路径（prove 行） |
| `src/core/hooks/state-next.ts` | `DEBUG_PHASE_TO_SKILL` 两张表 |
| `test/ts/commands-install.test.ts` | `for (const phase of ['diagnose','patch','prove','closeout'])` 循环；`expect(hotfix).toContain('prove')`；`expect(bugfix).toContain('./policies/scene-routing.md')`（而 bugfix 里的该引用已被删） |

**入口层待定**（问用户中）：保留两个薄入口（各自只留本通道独有内容）vs 合并为一个入口。
两个命令文件 `assets/zh/commands/maintance/{bugfix,hotfix}.md` 各 8 行，只做技能名转发；`flow.md` 已不再提 debug。

---

## debug 族入口下沉到 command（19:20 完成）

**决策（用户定）**：保留两个薄入口 / 合并为一个入口**两个方案都不采纳** —— 「直接写到 command 里」。

**reasoning**：`diagnose` 自己就在 Step 2.2 做场景分流、Step 2.5 写 `channel`，两个入口技能的
「预期通道」必然被它覆盖；两入口的装配表又完全相同 ⇒ 入口技能是纯冗余。
入口层的存在理由只剩触发词与菜单路由，那就该待在命令/菜单层。

**改动 20 个文件**：
- 入口：`commands/maintance/{bugfix,hotfix}.md` 重写（预期通道 + 交出到 `debug:diagnose` + 分流唯一判定处）；
  删 `skills/debug/{bugfix,hotfix}/`。
- 阶段技能：`diagnose`/`patch`/`closeout` 的 description 不触发项 + 内部 `prescribe`·`prove` 引用全清；
  `patch` 的 `next-phase` 固定 `closeout`、回流改为回 `diagnose`。
- 模板：`verification-template.md`（prove → patch 1.5 步）、`preservation-checklist.md` /
  `containment-options.md` / `release-runbook.md`（**把 `debug:hotfix` 当技能名写的地方改成 `channel=hotfix`**）。
- `_shared/{artifacts,scene-routing,capability-tiers}`：产物归属表、通道边界表（新增「加严」「工作区」两行 +
  「channel 不改变装配」警示）、Jira 读取段。
- `README.md` 全篇重写（mermaid / 技能一览 / 阶段详解 / 目录树 / 产物契约 / 使用路径 / 维护约定）。
- 路由与外引用：`flow.md`（菜单描述 · 入口表 · 需求内容消费表）、`adapters/command-registration.md`、
  `hard-stops.md`（H12 适用 skill、H14 的 triage 段）。
- 源码：`src/core/hooks/state-next.ts`（`DEBUG_PHASE_TO_SKILL` 收成单表，`channel` → `_channel`）、
  `src/core/assets/layout.ts` 注释。
- 测试：`commands-install.test.ts`（debug 族用例重写，新增「已删技能不得落盘」守卫）、
  `state-next.test.ts`、`skills-install.test.ts`。

**验证**：`commands-install` + `skills-install` + `state-next` + `install-layout` = **45/45 通过**
（含此前恒红的 commands-install debug 族用例）。全量 `npx vitest run`：6 失败 / 31 通过，
失败集中在 `agents-install` / `command-adapters` / `detect` / `file-system` / `openspec` / `task-state`
——**全部是本次未触碰的模块**（本次 src 只动了 state-next.ts 与 layout.ts 的注释），判为既有失败。

**工具教训**：BSD grep 不吃双引号内 `\|` —— 本次因此两次误判「M01/M04 不在 flow.md 里」，
实际 `grep -E` 一查就有。核验中文/M1 类内容一律 `grep -E` 或 ripgrep。

---

## artifacts.md 存活分析（19:27，用户问「没有它会怎样」）

**前置事实**：用户已把 `_shared/` 整个删掉，三个样板挪到 `diagnose/policies/{artifacts,capability-tiers,scene-routing}.md`
（即**只有 diagnose 读得到**）。顶层 `assets/zh/policies/` 里**没有** artifacts.md。

**逐块来源盘点**：
| 块 | 别处出处 | 删了会丢吗 |
|---|---|---|
| L3 跨段写权通则（只写自己的节 / 禁止就地改上游 / 回退 `regressions[]` 留痕） | 只有 `patch:138` 覆盖「方案不成立」一种情形 | **会丢** |
| L5-13 产物清单 + 归属段 | README §产物契约 + 各技能 Step | 不会 |
| L16 归档路径 + 回读 5 项 | `closeout/SKILL.md` Step 3（带 bash）+ `bugfix-report-template` | 不会 |
| L18-30 分段回填写明表 | 各技能 Step 零散；`diagnose-brief-template.md:4` 写「每段只写自己负责的节（**见下**）」而那文件里没有表 | **会丢**，且模板那句「见下」悬空 |
| L33 档案禁止美化 | `diagnose:11` / `diagnose-brief-template:4` / `bugfix-report-template:99` | 不会 |
| L34 报告禁止变流水账 | 无 | **会丢** |
| L36-38 RCA 九节摘要 | **完整骨架在 `diagnose/templates/rca-report-template.md`**，`diagnose:283` 只让读模板 | 不会（且属**双源**） |
| L40 改进项不进范围 | 九节在模板第 9 节；但「patch 的 diff 校验据此回退」只有这里 | 半丢 |

**两个比「删不删」更严重的现存问题**：
1. **3 处 HARD-GATE 指针断链**：`diagnose:18` / `patch:15` / `closeout:15` 都写「见 `./policies/artifacts.md`」，
   但 `_shared` 删掉后**只有 diagnose 拿得到**。安装器只拦 `..` 开头的路径，**断链无任何校验会发现**。
   ⇒ artifacts.md 承载的跨技能约束（分段写权、patch 的 diff 校验口径）**对 patch/closeout 已失效**。
2. **`test/ts/commands-install.test.ts:227` 现在是假绿**：它断言
   `debug/diagnose/policies/{capability-tiers,artifacts,scene-routing}.md` 存在，注释写「族级 `_shared/` 样板注入
   debug 族**每个叶技能**」——现在只是因为文件恰好躺在 diagnose 自己的 `policies/` 里才通过，
   `_shared` 注入已无内容可注入，patch/closeout 拿不到，**测试不再守它声称守的东西**。

---

## 删除 `_shared/` 分发逻辑（19:33）

**改了什么**：
- `src/core/install/skills.ts`：删掉 **Step 3b**（族级 `_shared/*.md` 注入该族每个叶技能 `policies/`），
  连带 job label `skill_family_policy_inject` 一并消失（全仓只此一处）。
- **保留** `shouldSkipSkillShortPath` 里的 `/_shared/` 跳过段——它不是分发逻辑，是防 phantom 叶技能：
  去掉它 `debug/_shared/artifacts.md` 会被解析成 family=debug、skill=`_shared` ⇒ 多出一个假技能。
  注释已改为「已退役的族级 `_shared/`（2026-09-17 取消分发）」。
- `collectSkillLeafRoots` 仍被 Step 3 使用（`skills.ts:365`），未变孤儿。
- `test/ts/commands-install.test.ts`：删掉 3 文件 `_shared` 注入断言 + `debug/_shared/SKILL.md` 不存在断言；
  保留「族专属策略不得泄漏到其他族」负例；用例标题去掉 `_shared`。
- `README.md`：目录树去掉 `_shared/` 块、新增 `diagnose/policies/`；「族级样板注入」条改为
  「策略文件位置」+ 写明代价；维护约定里 `_shared/` 注入 → 技能自带 `policies/`。

**刻意没做**：**没有在测试里钉死这三份策略的落点**。原因是用户正在并发移动它们
（截至 19:34：`artifacts.md` 已复制到 `diagnose|patch|closeout/references/`，
`capability-tiers.md`/`scene-routing.md` 在 `diagnose/policies/`）。位置定下来后再补断言。

**验证**：`tsc --noEmit` 零错；4 个关键测试 45/45；全量仍是 **6 失败 / 31 通过**，
失败集与改动前逐条一致（agents-install ×2、command-adapters、detect、file-system、openspec、task-state ×2）。

**环境提醒**：本仓库存在**第二个记忆根** `assets/zh/skills/.workbuddy/memory/`，
其中的 MEMORY.md / 2026-09-1x.md 仍描述「8 叶技能 + `_shared/` 3 族级样板 + 六阶段」的旧结构，
已与现状不符（本次未改）。
