# 2026-09-23 工作日志

## 上下文边界与压缩时机方案：批 1–4 已提交

承接 9-22 的方案与实施，本日完成提交（分支 `feature/coding-flow-optimization`）：

| 提交 | 内容 |
|---|---|
| `144f010` | `fix(skills)` 删除失效的 `context-recovery.md` + 修复 6 处路径与重复问题（批 1） |
| `d4e4caf` | `feat(core)` 上下文压缩的平台维度与 manual 出厂默认（批 2+3，12 文件） |
| `c4afb20` | `docs(skills)` 技能衔接协议改为 manual/auto 双模式（批 4） |
| `3f19561` | `docs` 设计提案 + 批 1–4 落地记录 + 工作记忆 |

提交后工作区干净。

## 工具教训（重要，会重复遇到）

**沙箱内 git 写操作不可用**：

- `.git/index.lock` 能**创建**但 **unlink 被沙箱拦截**（`Operation not permitted`），
  导致 `git add` / `git commit` 报 `index.lock: File exists`。
- 即使只读的 `git status` 也会创建 lock 并留下 stale lock（0 字节），
  下一次写操作就被挡住 —— 现象是「锁文件反复出现且删不掉」。
- **结论**：git 写操作必须用 `dangerouslyDisableSandbox: true`，并在命令前置 `rm -f .git/index.lock`。
- 另：`git commit -F - <<'EOF' … EOF` 的 heredoc 在本 shell 下**静默失败**（无输出、无错误、提交未发生）。
  改用多个 `-m` 参数（每个 `-m` 是一个段落），已验证可用。

**判别方法**：提交后必查 `git log --oneline -3` 确认 HEAD 已前移，不要以「命令无报错」当作成功。

---

## 批 5 完成：委派契约收紧（D2 取 B 案）

**用户决策**：D2 = B 案（收窄 D-2 + 反转保守策略）。

### 改动（`subagent-dispatch` + `prd/refine` + `prototype/ship`）
- **D-0.1 体积闸门（新增）**：D-2 只允许用于 `materials` 合计 ≤ 300 行的材料；超限 → 不注入，
  改换有读文件能力的 agent 或降级 `inline`。
- **D-0.2 保守策略反转**：旧文写「不确定宿主是否真授予 `tools` 时**应直接走内容注入型**」——
  这让「父代理先读全文」成为默认行为。现改为**不确定时走 D-1**，派发后若报 `NEEDS_CONTEXT`
  再按「换 agent → 闸门内 D-2 → inline」降级。
- **默认 subagent 分支**（`agent=null`，最常用路径）原本**硬编码走 D-2** → 改为受闸门约束。
  这是「父会话被灌入全文」的主要风险口。
- **回报契约收紧为三件**：`status` / `artifact_path` / `concerns`；禁止贴原文、代码块、diff、
  未命中检索、过程记录；产出必须先落盘再报路径。8 个 task_type 增强全部改为「写入 output_path → 只报路径」。
- **新增通用约定第 8 条**：产出路径取 `task_spec.constraints.output_path`，未指定则
  `<任务目录>/<task_type>-<timestamp>.md`。
- **消费方同步**：`prd/refine` 的 `result.output` → `result.artifact_path`，并给 4.1/4.2 补 `output_path`
  （`final/review-business-dispatch.md` / `final/review-testability-dispatch.md`）；
  `prototype/ship` 补「回报只给路径」；`subagent-dispatch/SKILL.md` 输出结构与 `constraints` 说明同步。

### 附带修正（自查发现）
- `isAutoTransitionEnabled` 上一轮重写时**漏了任务级 `false` 的否决**：`cfg === 'auto'` 会直接 return true，
  绕过 `taskState.auto_transition === false`。已重排为「显式关闭优先 → 开启来源判定 → 联动校验 → 兜底 manual」。
- **实现配置联动**（设计文档 §5.6 声明但未落码）：`auto_transition: 'auto'` 蕴含 `context_compression ≠ off`，
  非法组合直接降级 manual；补 3 条单测覆盖（合法 / 非法 / 任务级 auto）。

### 验证
`tsc --noEmit` 干净；`state-next` / `skills-install` / `session-start` / `subagent-capability` 全绿（56 passed）；
`commands-install` 4 例仍是既有失败，无新增。

