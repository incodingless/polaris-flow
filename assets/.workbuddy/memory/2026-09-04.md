# 2026-09-04 工作日志

## 开发工作流重构：design 阶段可选化（propose 内衔接）

参考 gstack 的"认知齿轮"思路，对 `assets/zh/skills/coding/` 开发工作流做第一阶段重构：**把 design（深度设计）从必经阶段改为 propose 完成后的可选衔接阶段**。

### 目标
- full（P03 完整链路）里 design 可选：propose 完成时询问"是否深化"，深化→design，跳过→直接 plan。
- tweak / normal 天然不经过 propose/design，无需此决策（已确认现状即如此，无需改动）。

### 断点诊断（原状态跟踪的 3 处硬编码）
1. `propose/SKILL.md` Step 5 写死 `--set phase=design` + 退出条件 `phase=design`。
2. `plan/SKILL.md` HARD-GATE + 入口校验硬编码 `detailed-design.md 存在 + design.status=completed`。
3. `verify/SKILL.md` 入口校验硬编码 `detailed-design.md 存在`。

### 修复方案（核心约定）
- propose 跳过时写 `design.status: skipped` + `design.review_report: "skipped:<reason>"`（**复用现有字段，不改 TS 类型**），直接 `--set phase=plan`。
- `state.example.yaml` 的 `design.status` 本就预留 `skipped` 语义，仅更新注释。
- **state-next.ts 无需改**：`PHASE_TO_SKILL` 是 phase→skill 静态映射，跳过 design 时 phase=plan 正确输出 plan skill。

### 改动清单（10 文件）
- `coding/propose/SKILL.md`：Step 5 拆出 5.1 深化设计决策点 + 5.2 按决策推进 phase；退出条件、description、上下文压缩恢复同步。
- `coding/plan/SKILL.md`：HARD-GATE、入口校验、规划依据表、Step 1 必读、Step 4.1 覆盖表、Step 6/6.2 materials、自审、上下文恢复全部条件化 detailed-design（`design.status=completed` 才依赖，`skipped` 降级为四件套推导）。
- `coding/verify/SKILL.md`：入口校验、完整验证检查项 3/6/7、规格漂移选项 A（skipped 时改写到 verify-report.md）。
- `coding/build/SKILL.md`、`coding/ship/SKILL.md`、`coding/tweak/SKILL.md`、`coding/normal/SKILL.md`：链路描述标注 design 可选。
- `README.md`：mermaid 图、阶段表、三条入口。
- `commands/polaris-flow.md`：P03 链路 + 复杂度判定。
- `shared/templates/state.example.yaml`：design.status 注释。

### 未改（刻意保留）
- `normal/policies/tier-gate.md` 的 `--set phase=design`：normal 升档 P03 语义=需要深化，保留。
- `state-next.ts`：phase→skill 映射无需变。
- `assets/zh/commands/normal.md` 有一处"文件末尾无换行"的预存在差异，非本次改动，未触碰。

### 遗留（后续阶段，本次未做）
- 第 2 点：verify→review 职责切分。
- 第 3 点：新增 test 阶段（API 测试 + E2E）。
- 第 4 点：P01/P02 是否同步改造（当前方案倾向 P01 不动、P02 轻调）。

### 上下文压缩交接
- 8 阶段重构完整蓝图（含 ②③④ 的决策点、倾向方案、命名配套、落地顺序）已沉淀到 `MEMORY.md` 的「8 阶段重构蓝图（gstack 思路，进行中）」章节，后续会话从那里恢复即可。

## ② verify→review：现状盘点 + 方案讨论（未动工，用户拍"暂时不改"）

用户选择"暂时不改动"，本次仅完成现状勘察与方案讨论，未改任何文件。下次从以下结论续。

### 本次勘察的新事实（补充 MEMORY 蓝图）
- **review 语义在系统已 3 处独立**：阶段评审 agent（plan/design/propose-review-agent + openspec-review-agent，审规格非代码）、build.final_review+requesting-code-review、state 顶层 `review` 子块（TaskReviewState 存 plan_review_status，给 plan 主审用）。verify→review 会在这 3 处撞车。
- `reviews/` 已堆 plan/design/propose-review-report.md；**`code-review-report.md` 已被 README「产物布局」与 build SKILL 文案预埋为"代码评审报告"** → 报告产物改名应走 `code-review-report.md`，不用裸 review-report.md（plan 侧已有 templates/review-report-template.md 裸名模板，再撞语义）。
- 代码层 verify 引用点：`state-next.ts` PHASE_TO_SKILL（verify:'verify'）、`task-state.ts` TaskVerifyState/TaskReviewState + 各阶段 review_report 字段、`polaris-project-config.ts` TaskPhase/VerifyMode。**红线：workflow-entry.ts 的 VerifySpec/verify 是"写后校验"语义，与阶段无关，不可误改**；openspec.ts 的 'verify' 是 OpenSpec CLI 自身 workflow，不可动。
- **TS 已滞后于文档**：normal/tweak state.yaml 已写 verify.status/mode/score_level/finished_at，但 TaskVerifyState 类型无这些字段 → 文档是事实，TS 需借机对齐。
- agent 源在 `assets/zh/agents/review/`（design/openspec/plan-review-agent + propose-reviewer，frontmatter tools/model/enabled 格式见 plan-review-agent.md）；session-start.ts `REVIEW_AGENTS` 数组列注入清单。design SKILL 有报告名不一致：标识约定写 design-review-report.md 但实际落 deep-design-review-report.md。
- 概念上 verify 是三合一（Constitution门禁+5scorer采集+light/full验收），scorer 是 retro 跨 change 数据源不可去。

### 用户拍板的方案决策（4+1）
1. 职责：**前置 hook + 深度独立评审**——Constitution+scorer 降为 build 后自动前置 hook，review 阶段=深审（单测覆盖+代码质量+意图一致性对照 specs）。
2. 报告命名：**code-review-report.md**（对齐 README 预埋路径，避开裸 review-report 与前置评审撞车）。
3. state：**复用改造顶层 `review` 块**承载 review 阶段状态（plan 主审的 plan_review_status 并入/另立子键）。
4. P01 不动（tweak 出口检查维持），P02 轻调（normal 出口检查同步新命名/块结构，不引入独立深审）。
5. 深审执行者：**新增 code-review-agent**（与既有 review agent 族平行，agent 源+session-start 注入+agent 清单同步注册）。
- scope：倾向本次只做②、不引入③test阶段；未落方案文档、未改代码。落地前建议先写 change-brief 一类设计文档。
