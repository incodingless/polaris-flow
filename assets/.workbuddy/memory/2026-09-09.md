# 2026-09-09

## polaris-flow.md 精简审查（zh/commands/polaris-flow.md，386 行 / ~11.7k 字）
- 结论：可精简约 15–20%，主要冗余为「发问后停下等待」约 8 处同义复述、需求三要素定义 3 处、P01–P03 档位描述 3 处重叠（第二步菜单/第四步阶段链表/文末复杂度判定表）、交接口径 3 处重复、快捷路径 vs 0.5 状态衔接重复。
- 客观错误 2 处：① 平台查表 `cursor` 重复出现 2 行；② 第二步维护菜单 M01 标「⚠️暂不可用」与第四步 M01→hotfix 完整路由矛盾（hotfix 含 bug 简报/tasks/写 fix/hard-gate 设计，应为可用）——待确认 hotfix 真实状态后修正其一。
- 建议方向：HARD-STOP 保留为唯一权威，正文复述改指针式引用；复杂度档位以文末表为唯一详表；结构性大改（零步抽独立策略文件）暂不列为本次动作。
- 状态：分批逐条确认推进。第一批已改：① 删除平台表重复的 cursor 行；② 核实 zh/skills/coding/hotfix/（SKILL.md+policies+templates 齐全）后，去掉第二步菜单 M01 的「⚠️ 暂不可用」过期标注。
- 第二批 2a 已改（7 处）：职责边界段并入引言；第一步/第二步/第三步的「停下等待」复述改为 HARD-STOP 指针；3.2 收集规则删首条冗余；第四步暂不可用段压缩并删尾句。HARD-STOP 与 0.2/通用约束未动。文件 386→376 行。
- 2b 已改（4 处）：「需求三要素」唯一定义落在零步开头，快捷路径与 0.1 判定规则 1 改为引用；0.5 状态衔接改为「与快捷路径一致 + 仅两处差异」（第三步单问变体、直接进第四步）。注意：0.2 选项 description 保持自包含（user-facing 文本，不引用内部章节）；去重后显露出既有行为差异——快捷路径走完整第三步 vs 0.5 走单问变体，属原设计，未改语义，待用户决定是否统一。剩余 2c（P01–P03 档位以文末表为唯一详表 + 交接口径三合一，含 0.5 尾部 vs 第四步的「技能消费需求内容」清单重复）。
- 2c 已改（7 处，全部完成）：第四步路由表 P01–P03 阶段链改指针，P03 完整链路并入文末《功能类选项的复杂度判定》并声明其为唯一详述出处；3.3 成为交接口径唯一权威（补入「需求内容」字段），第四步交接段、0.5 尾部清单、第五步第 1 条均改指针。最终 386→372 行 / 11697→11478 字符（约 -2%）：原估 15–20% 偏乐观，冗余点每处都很小且新增了指针与唯一定义文本，实际收益主要是「单一出处」的一致性而非篇幅。
- 踩坑：对同一文件并行发多个 Edit 调用会发生写覆盖竞争（各自基于编辑前内容整文件写回，last-writer-wins，其余编辑静默丢失且工具仍报成功）——同文件多处编辑必须串行，或改用脚本原子化应用+逐条断言 count==1。2b/2c 均已改用脚本并 grep 验证。
- 遗留待决已决第①项：第三步行为统一——删除 0.5 的单问变体（变体与 3.1 同为一次询问调用、不省交互），快捷路径与自动评估路径现均走完整 3.1/3.2，与 HARD-STOP #8 措辞对齐。文件终态 369 行 / 11424 字符（原 386 / 11697）。待决第②项（零步抽独立策略文件）未动，用户未要求。
- 待决第②项（零步抽独立策略文件）未动，用户未要求。

## verify 技能重构设计稿 v1
- 起点：现有 verify 技能（SKILL.md + policies/constitution-audit.md）覆盖了 Constitution 审计 + 5 scorer + light/full 二分；但对照《AICoding 中 Verify 最佳实践》PDF 的 L1-L5 + 6 层闭环，仅覆盖约 30%。
- 设计方案：把 verify 拆为 5 子阶段 A 前置契约 / B 代码审查（mandatory）/ C 意图验收（mandatory）/ D 功能测试（OPTIONAL）/ E 证据归档。功能测试按 verify_mode × risk_tier 决策表自动开关，仅 full + tier3 不可关闭。
- 风险分级推断：按文件路径 glob 推 tier1/2/3（auth/payment/crypto/concurrency/migration/infra/secrets → tier3；api/services/models/repositories/controllers → tier2；其余 tier1）。允许用户 `--risk-tier` 显式覆盖。
- 状态机扩展：state.yaml 新增 risk_tier / functional_test / contract / review / intent / functional / no_verification 嵌套段；metrics JSON 新增 verify_mode / risk_tier / functional_test_enabled / intent_match / functional / no_verification 字段。
- 证据闭环关键项：verify-report.md 新增「No-Verification 声明」段（结构化列出未跑项 + 原因），与 PDF「证据即交付」原则对齐。
- 与 PDF 原则映射校验：生成与裁决分离（B1 由独立模块执行；C 不调 build agent；D4 变异测试由独立工具）/ 自报告不是证据（每结论附日志）/ 验证悖论破解（D1 历史用例 + D4 变异 + D5 多模型）/ 风险分级（§3 决策表）。
- 落地顺序建议：先确认 §2 顶层 5 子阶段边界 → §3 开关策略 → §4 产物 → §5 决策点 → §7 改造点 → §6 原则映射。增量推进，一次敲定一个。
- 设计稿文件：`assets/zh/skills/coding/verify/verify-redesign-proposal.md`（v1，待用户确认）。
- 待用户决策 Q1-Q5：risk_inference 配置机制 / 多模型交叉是否入 v1 / decision-point.md 是新建全局还是各技能维护 / .polaris/knowledge/ 是否新建 / functional_test 关闭后 C4 spec scenario 追溯要求。

## verify 职能方案定稿（与用户逐点敲定）
- 职能方案（4 个验证环节，去掉独立证据归档环节）已定稿：
  1. 前置契约（必需）— 验收前提是否成立（spec 冻结/build 完成/tasks 勾完/术语映射）
  2. 意图验收（必需）— 对照 proposal/design/detailed-design/spec 核验方向（Validation·拦方向）
  3. 代码审查（必需）— Constitution 合规/静态/安全/幻觉/复杂度（Verification·拦质量）
  4. 功能测试（可选）— 旧轨+新轨+契约测试，风险分级自动开关
- 顺序：**意图验收前置**（先验方向再验质量），用户主动提出「意图检查放第2步」，理由是 PDF 4.3「AI 最容易错在业务语义而非技术实现」+ 测试左移。
- 边界：detailed-design 符合性归「意图验收」（它是设计规格，对照它是验方向不是验质量）。
- 功能测试可选语义 = 风险分级自动开关（低风险默认跳过、高风险强制、中风险可覆盖），非纯手动。
- 证据归档：**去掉独立环节**，证据落盘分散到各环节的产出义务，出口统一汇总到 verify-report.md + metrics + 未验证项声明。
- 设计稿文件已同步 §1.3/§2 为 4 环节 + 新顺序；§5/§6/§7/§9 的 E 引用待落地时统一改。
- SKILL.md 未动，等整体方案确认后再落地。
- 下一轮待议（技术落地细节）：Q1 已决（config.yaml 覆盖机制）；Q2 多模型交叉是否入 v1、Q3 decision-point.md 归属、Q4 知识回流去向（E 去掉后）、Q5 functional_test 关闭后 spec scenario 追溯要求。

## verify 综合方案 v2（现有设计 + 报告思路融合，用户纠偏）
- 用户纠偏：「当前技能本身的功能设计不能丢弃」，要用「现有设计 + 报告思路」形成更综合的方案，而非推倒重来。
- 关键认知修正：之前「4 环节」抽象易给人「打散重来」错觉。真实情况是——意图验收/代码审查/功能测试从来不是新造，而是现有 Step 4.2b（意图）、Step 2+3（宪法+scorer=代码审查）、Step 4（相关测试=功能测试）里已有功能的「重新归类+增强」。
- 融合三条铁律：① 保留优先（Step 0-5 一条不丢）② 增量有取舍（纳入/裁剪/推迟三档）③ 调整最小化（仅两处结构调整）。
- 综合方案：现有 Step 主线 + 报告增量注入 + 2 处调整。调整1=意图验收前置（Step 4.2b 的 5 项 proposal/design/detailed-design/spec/漂移 原样搬到 Step 2，内容零增删）；调整2=功能测试增强为可选（保留 light/full，+双轨/契约/L4/风险分级）。
- 报告增量三档评估：纳入=前置契约/AI幻觉检测/双轨测试/契约测试/风险分级/No-Verification声明；裁剪=SAST（规则化清单不引重型工具）/L4（仅 tier3 启用阈值可配）；推迟=多模型交叉（v2 留接口）。
- 设计稿已重写为「综合方案 v2」，结构：0 融合原则 / 1 现有功能资产清单 / 2 报告增量三档 / 3 综合方案Step主线 / 4 开关策略 / 5 产物 / 6 决策点 / 7 PDF映射 / 8 改造点 / 9 落地顺序 / 10 待决。
- 落地顺序建议：Step2意图前置 → Step4功能测试+分级 → Step0前置契约 → Step3幻觉+SAST → Step5 No-Verification → policies拆分。SKILL.md 仍未动。
