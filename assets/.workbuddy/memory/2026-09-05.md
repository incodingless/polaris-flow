# 2026-09-05 工作日志

## 审计：coding 工作流 state.yaml 更新逻辑 vs 新 state.example.yaml

用户指定 `shared/templates/state.example.yaml`（未提交改动，16:03）为 coding 技能 state.yaml 的新模板基准，要求核对各阶段技能的 state 写入逻辑是否与新结构一致。

### 新模板关键结构（本次改动内容）
- 把原先分离的「产物信息」与底部「运行时数据」两节**合并为单一 schema**，start_time/finished_at 折进各产物块。
- `intention:` 块更名 → `clarify:`（含 `intention_path`/`status`/start/finish）。
- 阶段块：clarify/propose/design/plan/**review**/build/**delivery**/deepread。
- phase 枚举：idle|clarify|propose|design|plan|build|verify|**delivery**（无 ship）。
- workflow 枚举：sdd|tweak|bugfix|full。

### 技能侧实际写入（漂移点）
- 各 skill 每步写 `current_verb`（顶层，模板无此键）。
- P03: design→plan→build→verify→**ship**；verify/tweak/normal 收尾都 `--set phase=ship`。
- skill 写 `verify.*`（status/overall_score/blocked/scorer_results…）与 `ship.*`(delivered/merge_strategy/harness_sync/archive*/backfill)、`normal.*`/`tweak.*`/signals/tdd_mode/build_mode —— 这些块模板均不存在，唯一近似物是模板 `delivery` 块（字段与 ship 的 archive 部分一致但父键不同）。
- 顶层 phase 枚举与 skill 推进值不一致：模板无 `ship`。

### 结论
模板与技能 state 写入**不一致**。根因：模板仍沿用 `delivery` 命名与「无 verify 阶段块、无 normal/tweak 档位块」的旧模型；而技能文档层已整体迁到 `ship` + `verify` 块 + P01/P02/P03（normal/tweak）。与记忆里既有「代码层用 delivery / 文档层用 ship，子块不被代码读取」的判断一致 —— 属文档/类型不一致，不影响运行。

### 待决（未改文件，仅审计）
方向二选一：(A) 把模板改齐到 ship+verify+normal/tweak 模型（对齐已迁的文档与 8 阶段蓝图）；(B) 把技能写回 delivery/review/无档位块（回退，不推荐，与 P01/P02/P03 冲突）。倾向 A。另：模板 `review` 顶层块与技能「无独立 review 阶段块」（评审经 subagent 折叠进 propose/design/plan）也不对齐，需在 review 阶段改造时一并定。

## 复核（15:0x）：逐文件全量核对，补充上述审计未覆盖项

产出报告：`tmp/state-yaml-audit-2026-09-05.md`。在上一节基础上新增：

### 上一节遗漏的关键点
- **模板新增了 `runtime:` 容器**：所有阶段块（clarify/propose/design/plan/review/build/delivery/deepread）都嵌在 `runtime:` 下，而技能全部写成顶层 `propose:` / `design:` / `plan:` / `build:` —— 这不只是命名漂移，是层级错位。
- `current_verb` 共 **22 处**（精确到行号已入报告），分布在 10 个文件。
- 模板 propose 块有 4 个字段**从无技能写入**：`worktree_decision` / `opsx_propose_status` / `review_round` / `review_log_file`。
- 所有阶段块的 `start_time` **零写入**（技能只写 finished_at）；`triage.*`、`worktree.created_at`、`runtime.deepread.*`、`runtime.review.*`、`artifact_max_round`、`workflow` 均无人写入；`current_tier` 只被 propose 3.3.1 读、从不写。

### 3 个已确认的功能性 bug（上一节未发现）
1. `ship/SKILL.md:102` 条件写 `worktree.created_by_easy_flow == true`（全仓唯一一处旧名，其余 8 处为 `created_by_polaris_flow`）→ **ship Step 3 产物合回+清理整段永不执行**，`.polaris/` 产物随 worktree 删除永久丢失。
2. `ship/SKILL.md:203` + `ship|tweak/policies/artifact-backfill.md:61` 读 `tweak.mode == "tweak"`，模板无 tweak 块 → **P01 产物补齐永不触发**，后续 `openspec-cn archive` 因四件套缺失失败。
3. `design/SKILL.md:194` 落盘 `reviews/deep-design-review-report.md`，但 L236 与 `plan/SKILL.md:102` 引用 `reviews/design-review-report.md` → **plan 入口审查门禁静默失效**。

### 遗留待用户决策（增量推进，先定第 1 项）
1. verify 块归属：新增 `runtime.verify` 还是并入 `runtime.review`？
2. ship→`runtime.delivery` 重命名，`status: delivered` 与 `backfill` 是否补进模板？
3. tweak/normal 私有段：补进模板还是改用顶层 `workflow` + `triage` 表达？
4. `build.build_mode` / `review_mode` / `final_review` 是否补进模板？
5. 确认 `current_verb: ship` → `phase: delivery`。

## 决策落地（15:43：用户 5 项决策全部敲定，开始批量修改）
1. verify 段归属 → 新增 `runtime.verify`（独立块，与 review 解耦）
2. ship → `runtime.ship`，`backfill` 字段补进模板
3. tweak/normal 私有段 → 补 `runtime.tweak` / `runtime.normal` 进模板
4. build 私有字段 → `build_mode` / `review_mode` / `final_review` 全补进模板
5. `current_verb` → `phase`，`delivery` → `ship`（与第 2 项对齐）

### 已完成的修改
- **批量**：所有 `current_verb:` → `phase:`，已 22 处全部替换
- **批量**：所有阶段字段引用加 `runtime.` 前缀（propose/design/plan/build/verify/ship/tweak/normal 等共 90+ 处）
- **嵌套**：10 个阶段 SKILL.md 内的 state yaml 块把 `propose:/design:/plan:/build:/verify:/ship:/tweak:/normal:` 全部移入 `runtime:` 容器（22 个 yaml 块 0 失配）
- **bug 修复**：
  - `ship/SKILL.md` 全仓唯一 `created_by_easy_flow` → `created_by_polaris_flow`（Step 3 不再静默跳过）
  - `design/SKILL.md` 落盘路径 `reviews/deep-design-review-report.md` → `reviews/design-review-report.md`（plan 入口审查门禁恢复）
  - `zh/policies/hard-stops.md` 同步修
- **模板 `shared/templates/state.example.yaml`**：
  - phase 枚举 `delivery` → `ship`
  - `runtime.delivery` → `runtime.ship`，加 `backfill`
  - 新增 `runtime.verify`、`runtime.tweak`、`runtime.normal`
  - `runtime.build` 加 `build_mode` / `review_mode` / `final_review`

### 最终校验（5 项全过）
1. 模板 YAML 解析合法
2. runtime 段齐：build/clarify/deepread/design/normal/plan/propose/review/ship/tweak/verify
3. phase 取值全部合法（idle/propose/build/design/verify/ship/plan 共 22 处，无 delivery 残留）
4. coding 目录内 current_verb/easy_flow/deep-design-review-report/phase=delivery 全清零
5. 技能引用的 runtime.* 字段全部在模板内，零失配

### 范围外（后续任务）
- `prd/draft/SKILL.md:50` 与 `prd/README.md:106` 仍有 `current_verb: draft`，属 prd 工作流，coding 已清零，不在本次范围
- `prd-final/SKILL.md:13` 英文短语 "delivery chapters" 是 PRD 章节语义，非 state 阶段名，无需改

## 关键纠正（15:59：tweak/normal 应在 workflow 块而非 runtime）
用户指出：tweak / normal 是**工作流**而非技能，把它们放在 `runtime` 下语义错位。runtime 是阶段容器，tweak/normal 应归属工作流层级。

### 改动
- **模板**：`workflow: sdd`（字符串）→ `workflow: { mode, tweak, normal }`（块）
  - 删除原 `runtime.tweak` / `runtime.normal` 段
  - 新增 `workflow.tweak.{mode,status,tdd_mode,build_mode,signals,upgrade_reason,upgrade_target,finished_at}`（8 字段）
  - 新增 `workflow.normal.{mode,status,tdd_mode,build_mode,signals,downgrade_reason,downgrade_target,upgrade_reason,upgrade_target,finished_at}`（10 字段）
- **批量**：`runtime.tweak.*` → `workflow.tweak.*` 共 6 处；`runtime.normal.*` → `workflow.normal.*` 共 2 处
  - 涉及：`tweak/SKILL.md`、`tweak/policies/upgrade-check.md`、`tweak/policies/artifact-backfill.md`、`ship/SKILL.md`（2 处）、`ship/policies/artifact-backfill.md`、`normal/SKILL.md`、`normal/policies/tier-gate.md`

### 最终校验（5 项全过）
1. 模板 YAML 合法
2. workflow 已升级为 dict：`mode=sdd`，tweak 8 字段，normal 10 字段
3. runtime 已无 tweak/normal（9 个段：build/clarify/deepread/design/plan/propose/review/ship/verify）
4. 技能引用全部合法（70 个 runtime 路径 + 18 个 workflow 路径 = 88 个，0 失配）
5. 残留扫描：`runtime.tweak`/`runtime.normal`/`current_verb`/`easy_flow`/`deep-design-review-report` 全部清零

### 设计要点（备忘）
- `workflow.mode` 是工作流类型选择入口（sdd/tweak/bugfix/full）
- `workflow.tweak.*` 仅 mode=tweak 时填充；`workflow.normal.*` 仅 mode=sdd 时填充
- 升档/降档信号（`signals` / `upgrade_reason` / `downgrade_reason`）聚合在工作流块下，因属工作流级决策
- 阶段段（runtime.propose/design/plan/build/...）继续承担阶段状态，与工作流块解耦

## 第四轮（16:30：消除孤儿字段 + propose 4 字段补写入）

### 孤儿段 `runtime.review` 删除（用户决策 B）
模板里有 `runtime.review.{status, plan_review_status, constitution_compliance, review_report, outside_voice, outside_voice_report}` 但零技能引用。

字段迁移：
- `plan_review_status` → `runtime.plan.plan_review_status`
- `constitution_compliance` → `runtime.verify.constitution_compliance`（与 `constitution_valid` 聚合判定并存）
- `review_report`/`outside_voice`/`outside_voice_report` 已在各阶段块，无需迁移
- 删除 `status`（review 不再是独立阶段）

### propose 4 个孤儿字段补写入（用户决策 第 2 项「补充写入」）
模板已有但零技能写入：

| 字段 | 写入点 | 取值 |
|---|---|---|
| `runtime.propose.worktree_decision` | Step 1.3.A 末 / 1.3.B 末 | `created` / `declined` |
| `runtime.propose.opsx_propose_status` | Step 3.4 末 | `ok` / `failed`（未执行可填 `n/a`）|
| `runtime.propose.review_log_file` | Step 3.3.2 用户决策点后 | A:`<path>` / B:`skipped:after_all` |
| `runtime.propose.review_round` | 3.3.2 初始化为 0；4.2 每轮 +1 | `0` → `1` → `2` |

实施位置：`zh/skills/coding/propose/SKILL.md`
- Step 1.3.A / 1.3.B 加写入 `worktree_decision`
- Step 3.3.2 表后补 `review_round` / `review_log_file` 同步写入
- Step 3.4 末补 `opsx_propose_status`
- Step 4.2 末补 `review_round + 1`
- Step 5 yaml 完成态块同步展示 4 字段（避免「先写后忘」漂移）

### 最终校验（5 项全过）
1. 模板合法
2. Step 5 yaml 块4 个新字段全部出现
3. 4 字段在 propose/SKILL.md 中出现次数：worktree_decision×3 / opsx_propose_status×2 / review_round×4 / review_log_file×2
4. 路径 0 失配（运行时.路径共 66 个合法）
5. 残留扫描：`runtime.review`/`runtime.tweak`/`runtime.normal`/`easy_flow` 全部 0；`current_verb` 仅 1 处在 prd/README.md（范围外）


## 第三轮纠正（16:06：runtime.review 段删除，字段分散到 plan/verify）
用户决策 B：删除 `runtime.review` 整段，相关字段按职责拆分到对应阶段块。

### 字段迁移
- `runtime.review.plan_review_status` → `runtime.plan.plan_review_status`（plan 阶段内 subagent 评审产出）
- `runtime.review.constitution_compliance` → `runtime.verify.constitution_compliance`（constitution 子规则逐条合规结果）
- `runtime.review.constitution_valid` 已存在于 `runtime.verify`（聚合判定），与新增的 `constitution_compliance` 并存（细则 + 汇总）
- `runtime.review.review_report` / `outside_voice` / `outside_voice_report` 已存在于 `runtime.propose` / `design` / `plan`，无需迁移

### 改动量
- 模板删除 `runtime.review` 整段（21 行）
- `runtime.plan` 加 `plan_review_status` 字段
- `runtime.verify` 加 `constitution_compliance` 字段
- **技能侧零改动**——因为之前已经确认 `runtime.review.*` 在全 coding 目录零引用（孤儿段，技能从未写入）

### 最终校验（5 项全过）
1. 模板 YAML 合法
2. runtime 段已无 review（剩 8 个：build/clarify/deepread/design/plan/propose/ship/verify）
3. plan 含 plan_review_status，verify 含 constitution_compliance + constitution_valid
4. 路径合法性 66 runtime + 18 workflow = 84 个，0 失配
5. 残留扫描：runtime.review / runtime.tweak / runtime.normal / 历史字段名全部清零

## 第五轮（20:50：polaris-flow TS 源码改造，对齐新 state.example.yaml）

用户选 A：全面改造 polaris-flow TypeScript 源码，让代码层也适配 runtime.* / workflow.* 新结构。

### 改造范围
1. **`src/core/config/task-state.ts`** —— 类型 + 默认值全量重写
   - 删 `clarify`/`intention`/`review`/`delivery` 顶层块
   - 新增 `runtime: TaskRuntimeState` 容器（8 段：clarify/propose/design/plan/build/verify/ship/deepread）
   - `workflow: string` → `workflow: TaskWorkflowState`（含 mode/tweak/normal）
   - 加 `workflow_state` 顶层字段（与 `workflow` 双写兼容老 reader）
   - 删 `review_mode`/`build_mode` 顶层（移入 `runtime.build`）；新增 `artifact_review_mode`/`artifact_max_round` 顶层
   - `migrateLegacyShape()`：读取层兼容旧扁平 yaml（intention/clarify/review/delivery/build_mode/review_mode/workflow 字符串）

2. **`src/core/hooks/task.ts`** —— 写入路径改造（2 处）
   - `state.intention = { path }` → `state.runtime.clarify.intention_path = ...`
   - `state.clarify = { status, draft_dir }` → `state.runtime.clarify = { status, intention_path, start_time }`
   - finalize 中 `intention/clarify` 顶层 patch → `runtime: { clarify: {...} }`

3. **`src/core/config/polaris-project-config.ts`** —— TaskPhase 调整
   - `'delivery'` → `'ship'`；注释说明 `delivery/archive` 为读取层历史别名

4. **`test/ts/task-state.test.ts`** —— 测试用例改新结构
   - 新增 5 个测试：default 骨架 / kebab 兼容 / save 回读 / **旧扁平自动归一** / patch 不丢块
   - 修正导入路径：`config/polaris-paths.js` → `assets/polaris-paths.js`（之前就是错的）

5. **`scripts/check_state_yaml_three_way.py`** —— 三方一致性校验脚本（新增）
   - 模板 ↔ TS 类型 ↔ SKILL.md 引用三方校验
   - 段名 + 子字段两级校验

### 最终校验（4 项全过）
1. ✓ `tsc --noEmit`：编译无错误
2. ✓ `vitest run test/ts/task-state.test.ts`：5/5 测试全过（含旧扁平兼容用例）
3. ✓ 三方一致性：模板/TS/SKILL 全部对齐（runtime 8 段、workflow 3 段、22 runtime 引用 + 5 workflow 引用 0 失配）
4. ✓ `state-next.ts` 的 `delivery/archive → ship` 别名保留（读取层向后兼容，写盘已统一为 ship）

### 关键设计决策
- **兼容策略**：读取层 `migrateLegacyShape` 自动归一旧扁平 yaml；写盘一律新结构。不维护双写。
- **kebab 兼容**：normalizeKeys 一律转 snake（删白名单），简化逻辑。
- **`workflow_state` 双写**：顶层同时写 `workflow: string`（兼容老 reader）和 `workflow_state: dict`（新结构权威）。读取时优先用 workflow_state。
